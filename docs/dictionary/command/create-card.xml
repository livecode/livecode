<doc>
  <legacy_id>1463</legacy_id>
  <name>create card</name>
  <type>command</type>
  <syntax>
    <example>create card [<i>cardName</i>]</example>
  </syntax>
  <library></library>
  <objects>
  </objects>
  <synonyms>
    <synonym>new card</synonym>
  </synonyms>
  <classification>
    <category>Objects &amp; Messages</category>
  </classification>
  <references>
    <command tag="create stack">create stack Command</command>
    <command tag="place">place Command</command>
    <command tag="clone">clone Command</command>
    <command tag="create">create Command</command>
    <keyword tag="templateCard">templateCard Keyword</keyword>
    <message tag="newCard">newCard Message</message>
  </references>
  <history>
    <introduced version="1.0">Added.</introduced>
  </history>
  <platforms>
    <mac/>
    <windows/>
    <linux/>
    <ios/>
    <android/>
  </platforms>
  <classes>
    <desktop/>
    <server/>
    <web/>
    <mobile/>
  </classes>
  <security>
  </security>
  <summary>Makes a new <keyword tag="card">card</keyword> with the same <object tag="group">background</object>(s) as the <glossary tag="current card">current card</glossary>.</summary>
  <examples>
    <example>create card</example>
    <example>card card "Employees"</example>
    <example>create card (field "Name" of card "Template")</example>
  </examples>
  <description>
    <p>Use the <b>create card</b> <glossary tag="command">command</glossary> to add a new <keyword tag="card">card</keyword> to the <glossary tag="current stack">current stack</glossary>.</p><p/><p><b>Parameters:</b></p><p>The <i>cardName</i> is the <property tag="name">name</property> <glossary tag="property">property</glossary> of the new <keyword tag="card">card</keyword>. If you don't specify a <i>cardName</i>, the new <glossary tag="card">card's</glossary> name is empty.</p><p/><p><b>Comments:</b></p><p>The new card is placed after the current card in the <b>defaultStack</b>. When the <keyword tag="card">card</keyword> is created, you go to the new <keyword tag="card">card</keyword>.</p><p/><p>Any groups on the current card whose <b>backgroundBehavior</b> is set to true will be placed on the newly-created <keyword tag="card">card</keyword> automatically.</p><p/><p>The <b>create card</b> <glossary tag="command">command</glossary> places the <property tag="ID">ID</property> <glossary tag="property">property</glossary> of the newly created <keyword tag="card">card</keyword> in the <keyword tag="it">it</keyword> <glossary tag="variable">variable</glossary>.</p><p/><p><code/><img src="202651"/> <b>Tip:</b><code/> To create a <keyword tag="card">card</keyword> in a specific <object tag="stack">stack</object>, first set the <property tag="defaultStack">defaultStack</property> to the <object tag="stack">stack</object> where you want to create the new <keyword tag="card">card</keyword>:</p><p/><p><code>  set the defaultStack to "My Stack"</code></p><p><code>  create card</code></p>
  </description>
</doc>