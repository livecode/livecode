<doc>
  <legacy_id>1436</legacy_id>
  <name>create stack</name>
  <type>command</type>
  <syntax>
    <example>create [invisible] stack [<i>stackName</i> [with <i>background</i>]]</example>
  </syntax>
  <library></library>
  <objects>
  </objects>
  <synonyms>
    <synonym>new stack</synonym>
  </synonyms>
  <classification>
    <category>Objects &amp; Messages</category>
  </classification>
  <references>
    <command tag="clone">clone Command</command>
    <command tag="create">create Command</command>
    <message tag="newStack">newStack Message</message>
    <command tag="save">save Command</command>
    <keyword tag="templateStack">templateStack Keyword</keyword>
    <command tag="create card">create card Command</command>
    <property tag="stackFileType">stackFileType Property</property>
  </references>
  <history>
    <introduced version="1.0">Added.</introduced>
  </history>
  <platforms>
    <mac/>
    <windows/>
    <linux/>
    <ios/>
    <android/>
  </platforms>
  <classes>
    <desktop/>
    <web/>
    <mobile/>
  </classes>
  <security>
  </security>
  <summary>Makes a new blank <object tag="stack">stack</object>.</summary>
  <examples>
    <example>create stack "Test"</example>
    <example>create stack (field 3) with background "Standard Navigation"</example>
  </examples>
  <description>
    <p>Use the <b>create stack</b> command to make a new <object tag="stack">stack</object>.</p><p/><p><b>Parameters:</b></p><p>The <i>stackName</i> is the name of the newly-created <object tag="stack">stack</object>. If you don't specify a <i>stackName</i>, the new <glossary tag="stack">stack's</glossary> name is the value of the <function tag="seconds">seconds function</function> at the time of creation.</p><p/><p>The <i>background</i> is a <href tag="reference/object_reference.rev">reference</href> to any <command tag="group">group</command> in the <property tag="defaultStack">defaultStack</property>. This <command tag="group">group</command>, if specified, is included in the new <object tag="stack">stack</object>.</p><p/><p><b>Comments:</b></p><p>The newly created stack is not saved to a file until you explicitly save it. If you close it without saving it, the stack disappears.</p><p/><p>If you use the create invisible stack form, the stack is created with its <b>visible</b> <glossary tag="property">property</glossary> set to false, so it cannot be seen. Use this form to create a hidden window, change its appearance or position, then make it visible.</p><p/><p>The stack created when create stack is executed is a clone of the templateStack.</p>
  </description>
</doc>