<doc>
	<legacy_id></legacy_id>
	<name>iphoneSetDoNotBackupFile</name>
	<type>command</type>

	<syntax>
		<example>iphoneSetDoNotBackupFile <i>filename [, doNotBackup]</i></example>
	</syntax>

	<synonyms>
	</synonyms>

	<summary>Sets the &quot;do not backup&quot; flag for the given file.</summary>

	<examples>
<example>iphoneSetDoNotBackupFile myFile, true</example>
<example>iphoneSetDoNotBackupFile specialFolderPath(&quot;documents&quot;) &amp; &quot;/appointments.txt&quot;, false</example>
	</examples>

	<history>
		<introduced version="5.5.1">Added</introduced>
		<deprecated version=""></deprecated>
		<removed version=""></removed>
		
	</history>

	<objects>
	</objects>

	<platforms>
		<ios/>
	</platforms>

	<classes>
		<mobile/>
	</classes>

	<security>
	</security>


	<classification>
	</classification>

	<references>
		<function tag="iphoneDoNotBackupFile">iphoneDoNotBackupFile Function</function>
		<function tag="iphoneFileDataProtection">iphoneFileDataProtection Function</function>
		<command tag="iphoneSetFileDataProtection">iphoneSetFileDataProtection Command</command>
		<message tag="protectedDataWillBecomeUnavailable">protectedDataWillBecomeUnavailable Message</message>
		<message tag="protectedDataDidBecomeAvailable">protectedDataDidBecomeAvailable Message</message>
	</references>

	<description>
	<overview>Use the <b>iphoneSetDoNotBackupFile</b> command to set a flag specifying whether a file should be backed up by iOS, or not.</overview>

		<parameters>
  <parameter>
    <name>filename</name>
    <description>The fulle path file to flag</description>
  </parameter>
  <parameter>
    <name>doNotBackup</name>
    <description>A boolean value specifying if the file should be backed up</description>
  </parameter>		</parameters>

	<value></value>
	<comments>The <b>iphoneSetDoNotBackupFile</b> command sets a flag specifying whenere a file should be backed up by iOS, or not.<p></p><p>If the <i>doNotBackup</i>parameter is true the file will not be considered for backup to the Cloud. If false the file will be treated normally. If the <i>doNotBackup</i> parameter is not given then the value of the flag will be set to true by default.</p></comments>
	</description>
</doc>