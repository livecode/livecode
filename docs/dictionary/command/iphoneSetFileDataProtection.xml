<doc>
	<legacy_id></legacy_id>
	<name>iphoneSetFileDataProtection</name>
	<type>command</type>

	<syntax>
		<example>iphoneSetFileDataProtection <i>filename, dataProtection</i></example>
	</syntax>

	<synonyms>
	</synonyms>

	<summary>Sets the data protection level of the given file.</summary>

	<examples>
<example>iphoneSetFileDataProtection myFile, &quot;none&quot;</example>
<example>iphoneSetFileDataProtection specialFolderPath(&quot;documents&quot;) &amp; &quot;/appointments.txt&quot;, &quot;complete&quot;</example>
	</examples>

	<history>
		<introduced version="5.5.1">Added</introduced>
		<deprecated version=""></deprecated>
		<removed version=""></removed>
		
	</history>

	<objects>
	</objects>

	<platforms>
		<ios/>
	</platforms>

	<classes>
		<mobile/>
	</classes>

	<security>
	</security>


	<classification>
	</classification>

	<references>
		<function tag="iphoneDoNotBackupFile">iphoneDoNotBackupFile Function</function>
		<function tag="iphoneFileDataProtection">iphoneFileDataProtection Function</function>
		<command tag="iphoneSetDoNotBackupFile">iphoneSetDoNotBackupFile Command</command>
		<message tag="protectedDataWillBecomeUnavailable">protectedDataWillBecomeUnavailable Message</message>
		<message tag="protectedDataDidBecomeAvailable">protectedDataDidBecomeAvailable Message</message>
	</references>

	<description>
	<overview>Use the <b>iphoneSetFileDataProtection</b> command to set the data protectio level of a file.</overview>

		<parameters>
  <parameter>
    <name>filename</name>
    <description>The fulle path to the file to be protected.</description>
  </parameter>
  <parameter>
    <name>dataProtection</name>
    <description>The level of protection to apply to the file</description>
    <options title="">
      <option>
        <item>none</item>
        <description>No protection</description>
      </option>
      <option>
        <item>complete</item>
        <description>The file is not accessible, for read or write, while the device is locked. </description>
      </option>
      <option>
        <item>complete unless open</item>
        <description>The file is fully protected when the device is locked, unless it was already open</description>
      </option>
      <option>
        <item>complete until first user authentication</item>
        <description>The file is fully protected until the user unlocks the device for this first time.</description>
      </option>
    </options>
  </parameter>		</parameters>

	<value></value>
	<comments>The  <b>iphoneSetFileDataProtection</b> command sets the level of data protection applied to a file.</comments>
	</description>
</doc>