<doc>
	<legacy_id></legacy_id>
	<name>iphoneSetReachabilityTarget</name>
	<type>command</type>

	<syntax>
		<example>iphoneSetReachabilityTarget <i>hostNameOrAddress</i></example>
	</syntax>

	<synonyms>
	</synonyms>

	<summary>Starts monitoring a server of reachability via the network.</summary>

	<examples>
<example>iphoneSetReachabilityTarget tServer</example>
<example>iphoneSetReachabilityTarget</example>
	</examples>

	<history>
		<introduced version="6.0">Added</introduced>
		<deprecated version=""></deprecated>
		<removed version=""></removed>
		<changed version=""></changed>
		<experimental version=""></experimental>
		<nonexperimental version=""></nonexperimental>
	</history>

	<objects>
		<stack/>
	</objects>

	<platforms>
		<ios/>
	</platforms>

	<classes>
		<mobile/>
	</classes>

	<security>
	</security>


	<classification>
	</classification>

	<references>
		<function tag="iphoneReachabilityTarget">iphoneReachabilityTarget Function</function>
		<message tag="reachabilityChanged">reachabilityChanged Message</message>
	</references>

	<description>
	<overview>Use the <b>iphoneSetReachabilityTarget </b>command to start monitoring a specific server for reachability via the network.</overview>

		<parameters>
  <parameter>
    <name>hostNameOrAddress</name>
    <description>The host name or IP address of the server to start monitoring, or empty to stop monitoring</description>
  </parameter>		</parameters>

	<value></value>
	<comments>The network connection on iOS devices is generally more transient than normal network connections and can change between wireless and wide-area wireless (GPRS, 3G, EDGE etc.) transport as it moves, and indeed be lost entirely.<p></p><p>As the behavior of an application may vary depending on what kind of network connection is present it is useful to be able to monitor a given server for the type of connection the device currently has to it.</p><p></p><p>To start monitoring a specific server for reachability via the network use the <b>iphoneSetReachabilityTarget</b> command.</p></comments>
	</description>
</doc>