<doc>
  <legacy_id>1279</legacy_id>
  <name>lock cursor</name>
  <type>command</type>
  <syntax>
    <example>lock cursor</example>
  </syntax>
  <library></library>
  <objects>
  </objects>
  <synonyms>
  </synonyms>
  <classification>
    <category>User Interaction</category>
  </classification>
  <references>
    <command tag="lock screen">lock screen Command</command>
    <property tag="cursor">cursor Property</property>
    <command tag="lock moves">lock moves Command</command>
    <command tag="lock colorMap">lock colorMap Command</command>
    <command tag="unlock cursor">unlock cursor Command</command>
    <property tag="lockCursor">lockCursor Property</property>
  </references>
  <history>
    <introduced version="1.0">Added.</introduced>
  </history>
  <platforms>
    <mac/>
    <windows/>
    <linux/>
  </platforms>
  <classes>
    <desktop/>
    <server/>
    <web/>
  </classes>
  <security>
  </security>
  <summary>Sets the <property tag="lockCursor">lockCursor</property> <glossary tag="property">property</glossary> to true, preventing the <property tag="cursor">cursor</property> shape from changing.</summary>
  <examples>
    <example>lock cursor</example>
    <example>if the mouse is not down then lock cursor</example>
  </examples>
  <description>
    <p>Use the <b>lock cursor</b> <glossary tag="command">command</glossary> to make the <property tag="cursor">cursor</property> retain a specified <glossary tag="appearance">appearance</glossary> until you change it.</p><p/><p><b>Comments:</b></p><p>The <b>lock cursor</b> <glossary tag="command">command</glossary> sets the <property tag="lockCursor">lockCursor</property> <glossary tag="property">property</glossary> to true.</p><p/><p>Set the <b>cursor</b> <glossary tag="property">property</glossary> to change the <property tag="cursor">cursor</property>.</p>
  </description>
</doc>