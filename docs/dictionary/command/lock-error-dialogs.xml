<doc>
  <legacy_id>2131</legacy_id>
  <name>lock error dialogs</name>
  <type>command</type>
  <syntax>
    <example>lock error dialogs</example>
  </syntax>
  <library></library>
  <objects>
  </objects>
  <synonyms>
  </synonyms>
  <classification>
    <category>Writing LiveCode</category>
  </classification>
  <references>
    <command tag="lock messages">lock messages Command</command>
    <command tag="unlock error dialogs">unlock error dialogs Command</command>
  </references>
  <history>
    <introduced version="1.0">Added.</introduced>
  </history>
  <platforms>
    <mac/>
    <windows/>
    <linux/>
  </platforms>
  <classes>
    <desktop/>
    <server/>
    <web/>
  </classes>
  <security>
  </security>
  <summary>Sets the <property tag="lockErrorDialogs">lockErrorDialogs</property> <glossary tag="property">property</glossary> to true, preventing the error window from being displayed when an <glossary tag="execution error">execution error</glossary> occurs.</summary>
  <examples>
    <example>lock error dialogs</example>
    <example>if the environment is among the lines of userEnv then lock error dialogs</example>
  </examples>
  <description>
    <p>Use the <b>lock error dialogs</b> <glossary tag="command">command</glossary> to <glossary tag="handle">handle</glossary> <glossary tag="execution error">execution errors</glossary> in a custom handler, rather than allowing LiveCode to display the standard error window.</p><p/><p><b>Comments:</b></p><p>The <b>lock error dialogs</b> <glossary tag="command">command</glossary> sets the <property tag="lockErrorDialogs">lockErrorDialogs</property> <glossary tag="property">property</glossary> to true.</p><p/><p>If an execution error occurs while the <b>lockErrorDialogs</b> <glossary tag="property">property</glossary> is set to true, an <message tag="errorDialog">errorDialog</message> <keyword tag="message box">message</keyword> is sent to the <glossary tag="object">object</glossary> whose <glossary tag="handler">handler</glossary> set the <property tag="lockErrorDialogs">lockErrorDialogs</property> to true.</p><p/><p>When all pending handlers are finished executing, the <b>lockErrorDialogs</b> <glossary tag="property">property</glossary> is set back to false, undoing the <b>lock error dialogs</b> <glossary tag="command">command's</glossary> action.</p>
  </description>
</doc>