<doc>
  <legacy_id>1082</legacy_id>
  <name>lock moves</name>
  <type>command</type>
  <syntax>
    <example>lock moves</example>
  </syntax>
  <library></library>
  <objects>
  </objects>
  <synonyms>
  </synonyms>
  <classification>
    <category>Images &amp; Multimedia</category>
  </classification>
  <references>
    <command tag="lock messages">lock messages Command</command>
    <command tag="move">move Command</command>
    <command tag="lock cursor">lock cursor Command</command>
    <command tag="unlock moves">unlock moves Command</command>
  </references>
  <history>
    <introduced version="1.0">Added.</introduced>
  </history>
  <platforms>
    <mac/>
    <windows/>
    <linux/>
    <ios/>
    <android/>
  </platforms>
  <classes>
    <desktop/>
    <server/>
    <web/>
    <mobile/>
  </classes>
  <security>
  </security>
  <summary>Sets the <property tag="lockMoves">lockMoves</property> <glossary tag="property">property</glossary> to true, preventing motions caused with the <command tag="move">move</command> <glossary tag="command">command</glossary> from being immediately visible.</summary>
  <examples>
    <example>lock moves</example>
    <example>if the lockScreen is false then lock moves</example>
  </examples>
  <description>
    <p>Use the <b>lock moves</b> command to synchronize motions made with the move command.</p><p/><p><b>Comments:</b></p><p>When you use the <b>move</b> command to move an object around the screen, the movement takes place immediately. If you want to move more than one object simultaneously, first use the <b>lock moves</b> command to prevent these motions from being seen. Then issue the necessary move commands to move the objects, appending "without waiting" to each. Finally, use unlock moves to show the result of the move commands.</p><p/><p>The <b>lock moves</b> command sets the lockMoves property to true.</p><p/><p>When all pending handlers are finished executing, the <b>lockMoves</b> property is set back to false.</p>
  </description>
</doc>