<doc>
	<legacy_id></legacy_id>
	<name>mobileLockOrientation</name>
	<type>command</type>

	<syntax>
		<example>mobileLockOrientation</example>
	</syntax>

	<synonyms>
		<synonym>iphoneLockOrientation</synonym>
	</synonyms>

	<summary>Temporarily prevents orientation changes.</summary>

	<examples>
<example>mobileLockOrientation</example>
<example>if loading then mobileLockOrientation</example>
	</examples>

	<history>
		<introduced version="4.5.3">Added.</introduced>
		<deprecated version=""></deprecated>
		<removed version=""></removed>
		<changed version="4.6.1">4.6.1</changed>
	</history>

	<objects>
	</objects>

	<platforms>
		<ios/>
		<android/>
	</platforms>

	<classes>
		<mobile/>
	</classes>

	<security>
	</security>


	<classification>
	</classification>

	<references>
		<function tag="mobileDeviceOrientation">mobileDeviceOrientation Function</function>
		<function tag="mobileOrientation">mobileOrientation Function</function>
		<function tag="mobileAllowedOrientations">mobileAllowedOrientations Function</function>
		<command tag="mobileSetAllowedOrientations">mobileSetAllowedOrientations Command</command>
		<command tag="mobileUnlockOrientation">mobileUnlockOrientation Command</command>
		<message tag="orientationChanged">orientationChanged Message</message>
	</references>

	<description>
	<overview>Use the <b>mobileLockOrientation</b> command to temporarily prevent orientation changes.</overview>

		<parameters>
		</parameters>

	<value></value>
	<comments>The <b>mobileLockOrientation</b> command temporarily prevents orientation changes, for example while your application is performing a task and you do not want the orientation to change until the task is complete.<p></p><p>The orientation lock can be nested, and when an unlock request causes the nesting to return to zero, the interface rotates to match the device's current orientation (assuming it is in the set of allowed orientations). You can query the current orientation lock state with the <function tag="mobileOrientationLocked">mobileOrientationLocked</function> function.</p></comments>
	</description>
</doc>