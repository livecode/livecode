<doc>
  <legacy_id>2046</legacy_id>
  <name>paste</name>
  <type>command</type>
  <syntax>
    <example>paste</example>
  </syntax>
  <library></library>
  <objects>
  </objects>
  <synonyms>
  </synonyms>
  <classification>
    <category>User Interaction</category>
  </classification>
  <references>
    <command tag="copy">copy Command</command>
    <command tag="clone">clone Command</command>
    <command tag="doMenu">doMenu Command</command>
    <function tag="clipboard">clipboard Function</function>
    <message tag="newButton">newButton Message</message>
    <message tag="newEPS">newEPS Message</message>
    <message tag="newPlayer">newPlayer Message</message>
    <message tag="newField">newField Message</message>
    <message tag="newScrollbar">newScrollbar Message</message>
    <message tag="newGraphic">newGraphic Message</message>
    <message tag="newImage">newImage Message</message>
    <message tag="newCard">newCard Message</message>
    <message tag="newGroup">newGroup Message</message>
  </references>
  <history>
    <introduced version="1.0">Added.</introduced>
    <changed version="2.9"></changed>
  </history>
  <platforms>
    <mac/>
    <windows/>
    <linux/>
  </platforms>
  <classes>
    <desktop/>
    <server/>
    <web/>
  </classes>
  <security>
  </security>
  <summary>Pastes the contents of the <function tag="clipboard">clipboard</function> into the <keyword tag="selection">selection</keyword> or <glossary tag="insertion point">insertion point</glossary>.</summary>
  <examples>
    <example>paste</example>
    <example>if the clipboard is "text" then paste</example>
  </examples>
  <description>
    <p>Use the <b>paste</b> <glossary tag="command">command</glossary> to place <glossary tag="object">objects</glossary>, an <keyword tag="image">image</keyword>, or text on the <glossary tag="current card">current card</glossary>.</p><p/><p><b>Comments:</b></p><p>If there is an active field (one that contains the caret) and a text format is on the clipboard, text will be pasted into the field.</p><p/><p>If there is an image selection (the image selection tool is active and a region of an image is selected) and image data is on the clipboard, then the image data will be pasted into the selection.</p><p/><p>If the <property tag="defaultStack">defaultStack</property> is modifiable and there are objects on the clipboard, they will be pasted onto the current card of the <property tag="defaultStack">defaultStack</property>.</p><p/><p>If the <property tag="defaultStack">defaultStack</property> is modifiable and there is image data on the clipboard, a new image object will be pasted onto the current card of the <property tag="defaultStack">defaultStack</property>.</p><p/><p>Unless overidden by script, the standard paste keyboard shortcuts will have the same acion as the paste command except that the pointer tool must be selected for objects to be pasted.</p><p/><p>The <b>paste</b> <glossary tag="command">command</glossary> is equivalent to choosing Edit menu<img src="202656"/>Paste.</p>
  </description>
</doc>