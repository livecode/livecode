<doc>
  <legacy_id>1155</legacy_id>
  <name>prepare</name>
  <type>command</type>
  <syntax>
    <example>prepare videoClip <i>filePath</i> [looping] [at <i>startPoint</i>]</example>
  </syntax>
  <library></library>
  <objects>
  </objects>
  <synonyms>
  </synonyms>
  <classification>
    <category>Images &amp; Multimedia</category>
  </classification>
  <references>
    <property tag="frameRate">frameRate Property</property>
    <function tag="movie">movie Function</function>
    <command tag="play">play Command</command>
  </references>
  <history>
    <introduced version="1.0">Added.</introduced>
  </history>
  <platforms>
    <mac/>
    <windows/>
    <linux/>
  </platforms>
  <classes>
    <desktop/>
    <server/>
    <web/>
  </classes>
  <security>
  </security>
  <summary>Preloads a movie into memory.</summary>
  <examples>
    <example>prepare videoClip "/Disk/Folder/Movie" at 100,100</example>
    <example>prepare videoClip ID 17 looping</example>
  </examples>
  <description>
    <p>Use the <b>prepare</b> <glossary tag="command">command</glossary> to speed up <glossary tag="execute">execution</glossary> of the <command tag="play">play</command> <glossary tag="command">command</glossary>.</p><p/><p><b>Parameters:</b></p><p>The <i>filePath</i> is the location and name of the <keyword tag="file">file</keyword> you want to play. If you specify a name but not a location, LiveCode assumes the <keyword tag="file">file</keyword> is in the <property tag="defaultFolder">defaultFolder</property>.</p><p/><p>The <i>point</i> specifies the center of the movie to be played, relative to the <glossary tag="current stack">current stack</glossary> window. If the <i>point</i> is not specified, the movie is positioned at the center of the <keyword tag="card">card</keyword>.</p><p/><p><b>Comments:</b></p><p>Use the <b>prepare</b> <glossary tag="command">command</glossary> to preload the movie at some time before it is needed. Then use the <command tag="play">play</command> <glossary tag="command">command</glossary> with identical <glossary tag="parameter">parameters</glossary> to actually play the movie. Since the movie is already <glossary tag="loaded into memory">loaded into memory</glossary>, it starts up instantly when the <command tag="play">play</command> <glossary tag="command">command</glossary> <glossary tag="execute">executes</glossary>.</p><p/><p>The parameters of the <b>prepare</b> <glossary tag="command">command</glossary> must be identical to those of the corresponding <command tag="play">play</command> <glossary tag="command">command</glossary>.</p>
  </description>
</doc>