<doc>
	<legacy_id></legacy_id>
	<name>print anchor</name>
	<type>command</type>

	<syntax>
		<example>print <i>anchor</i> name at <i>anchorPoint</i></example>
	</syntax>

	<synonyms>
	</synonyms>

	<summary>Defines a target for an internal hyperlink within a PDF document.</summary>

	<examples>
<example>print anchor &quot;section 1.1&quot; at 50,50</example>
<example>print anchor tAnchor at x,y</example>
	</examples>

	<history>
		<introduced version="4.5">Added.</introduced>
		<deprecated version=""></deprecated>
		<removed version=""></removed>
		<changed version=""></changed>
	</history>

	<objects>
	</objects>

	<platforms>
		<mac/>
		<windows/>
		<linux/>
	  <ios/>
  </platforms>

	<classes>
		<desktop/>
		<web/>
	  <mobile/>
  </classes>

	<security>
	</security>


	<classification>
		<category>Printing</category>
	</classification>

	<references>
		<command tag="cancel printing">cancel printing Command</command>
		<command tag="open printing to pdf">open printing to pdf Command</command>
		<command tag="print link">print link Command</command>
		<command tag="print bookmark">print bookmark Command</command>
	</references>

	<description>
	<overview>Use the <b>print anchor</b> command to define a target for an internal hyperlink within a pdf created using <command tag="open printing to pdf">open printing to pdf</command>.</overview>

		<parameters>
		<parameter>The <i>name</i> is a label used to identify the anchor</parameter>
		<parameter>The <i>anchorPoint</i> is an x,y coordinate pair</parameter>
		</parameters>

	<value></value>
	<comments>The print anchor command defines a target for an internal hyperlink in a PDF file created using the <command tag="open printing to pdf">open printing to pdf</command> command.<p></p><p>The <i>name </i>parameter is a label used to identify the anchor in related <command tag="print link">print link</command> commands, and <i>anchorPoint</i> is the location on the current page to which any such link should jump.</p></comments>
	</description>
</doc>