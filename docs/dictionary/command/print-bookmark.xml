<doc>
	<legacy_id></legacy_id>
	<name>print bookmark</name>
	<type>command</type>

	<syntax>
		<example>print [ unicode ] bookmark <i>title</i> [ with level <i>level</i> ] [ at <i>location</i> ] [ initially ( open | closed )]</example>
	</syntax>

	<synonyms>
	</synonyms>

	<summary>Creates a document outline in a PDF generated by the pdf printing commands.</summary>

	<examples>
<example>print bookmark &quot;Chapter 1&quot;</example>
<example>print bookmark &quot;Chapter 1.1&quot; with level 2 at 100,150</example>
	</examples>

	<history>
		<introduced version="4.6">Added.</introduced>
		<deprecated version=""></deprecated>
		<removed version=""></removed>
		<changed version="4.6.1">inital open/close state added</changed>
	</history>

	<objects>
	</objects>

	<platforms>
		<mac/>
		<windows/>
		<linux/>
		<ios/>
	</platforms>

	<classes>
		<desktop/>
		<web/>
		<mobile/>
	</classes>

	<security>
	</security>


	<classification>
		<category>Printing</category>
	</classification>

	<references>
		<command tag="cancel printing">cancel printing Command</command>
		<command tag="open printing to pdf">open printing to pdf Command</command>
		<command tag="print anchor">print anchor Command</command>
		<command tag="print link">print link Command</command>
	</references>

	<description>
	<overview>Use the <b>print bookmark</b> command to create a document outline in a PDF generated by the pdf printing commands.</overview>

		<parameters>
  <parameter>
    <name>title</name>
    <description>The name of the bookmark.</description>
  </parameter>
  <parameter>
    <name>level</name>
    <description>The depth at which the entry should be placed.</description>
  </parameter>
  <parameter>
    <name>location</name>
    <description>A point describing where on the current page the bookmark should refer.</description>
  </parameter>		</parameters>

	<value></value>
	<comments>The <b>print bookmark</b> command allows the creation of a document outline in PDFs generated by the pdf printing commands. Individual entries in the document outline are referred to as 'bookmarks'.<p></p><p>If the unicode adjective is specified, then title is interpreted as a UTF-16 encoded string, otherwise it is interpreted as the native (single-byte) text encoding.</p><p></p><p>If <i>level</i> is not present, then the entry is inserted at the level of the previously inserted item, defaulting to the top-most level if no previous entries have been made.</p><p></p><p>If <i>location</i> is not present, the top-left of the current page is taken as the location.</p><p></p><p>If an initially clause is present it determines the default 'open' state of the bookmark item. Bookmarks are open by default.</p></comments>
	</description>
</doc>