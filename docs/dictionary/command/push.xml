<doc>
  <legacy_id>1459</legacy_id>
  <name>push</name>
  <type>command</type>
  <syntax>
    <example>push <i>card</i></example>
  </syntax>
  <library></library>
  <objects>
  </objects>
  <synonyms>
  </synonyms>
  <classification>
    <category>Navigation &amp; Hypertext</category>
  </classification>
  <references>
    <command tag="go">go Command</command>
    <property tag="ID">ID Property</property>
    <property tag="lockRecent">lockRecent Property</property>
    <command tag="pop">pop Command</command>
  </references>
  <history>
    <introduced version="1.0">Added.</introduced>
  </history>
  <platforms>
    <mac/>
    <windows/>
    <linux/>
    <ios/>
    <android/>
  </platforms>
  <classes>
    <desktop/>
    <server/>
    <web/>
    <mobile/>
  </classes>
  <security>
  </security>
  <summary>Places a card's long <property tag="ID">ID</property> in a list, from which it can be retrieved later with the <command tag="pop">pop</command> <glossary tag="command">command</glossary>.</summary>
  <examples>
    <example>push card <code><i>-- pushes current card</i></code></example>
    <example>push card "Starting Point"</example>
  </examples>
  <description>
    <p>Use the <b>push</b> <glossary tag="command">command</glossary> to store one or more locations for later use, or to keep a reference to the <glossary tag="current card">current card</glossary> so you can return to it after temporarily visiting another <keyword tag="card">card</keyword>.</p><p/><p><b>Parameters:</b></p><p>The <i>card</i> is any <href tag="reference/object_reference.rev">card reference</href>.</p><p/><p><b>Comments:</b></p><p>When you push a card, its <b>ID</b> is placed at the top of the list. The next time you use the <command tag="pop">pop</command> <glossary tag="command">command</glossary>, that <glossary tag="card">card's</glossary> ID is deleted from the list, and the previously-pushed <keyword tag="card">card</keyword> becomes the first <keyword tag="line">line</keyword> of the list. In this way, you can store multiple locations with the <b>push</b> <glossary tag="command">command</glossary>, and use the <command tag="pop">pop</command> <glossary tag="command">command</glossary> to revisit them in reverse order.</p><p/><p>The list of pushed cards is cleared when you quit the application.</p><p/><p>The Home card (the first card of the "license.rev" stack) is always the last card on the list.</p>
  </description>
</doc>