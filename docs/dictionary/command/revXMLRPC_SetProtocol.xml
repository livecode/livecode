<doc>
  <legacy_id>1232</legacy_id>
  <name>revXMLRPC_SetProtocol</name>
  <type>command</type>
  <syntax>
    <example>revXMLRPC_SetProtocol <i>XML-RPC document</i>,<i>protocol</i></example>
  </syntax>
  <library>XML-RPC library</library>
  <objects>
  </objects>
  <synonyms>
  </synonyms>
  <classification>
    <category>Text and Data Processing</category>
    <category>Networks &amp; Communication</category>
  </classification>
  <references>
    <function tag="revXMLRPC_CreateRequest">revXMLRPC_CreateRequest Function</function>
    <function tag="revXMLRPC_GetProtocol">revXMLRPC_GetProtocol Function</function>
  </references>
  <history>
    <introduced version="2.5">Added.</introduced>
  </history>
  <platforms>
    <mac/>
    <windows/>
    <linux/>
  </platforms>
  <classes>
    <desktop/>
    <server/>
    <web/>
  </classes>
  <security>
    <disk access="???"/>
    <process/>
  </security>
  <summary>Sets the protocol of an XML-RPC document structure.</summary>
  <examples>
  </examples>
  <description>
    <p>Use the <b>revXMLRPC_SetProtocol</b> <glossary tag="command">command</glossary> to set the protocol that is used for the connection during the execution of an <glossary tag="XML-RPC document">XML-RPC request</glossary>.</p><p/><p><b>Parameters:</b></p><p>The <i>documentID</i> is the number <glossary tag="return">returned</glossary> by the <function tag="revXMLRPC_CreateRequest">revXMLRPC_CreateRequest</function> when you created the <glossary tag="XML-RPC document">XML-RPC request</glossary>.</p><p/><p>The <i>protocol</i> determines the type of connection that is established with the host. By default, XML-RPC uses <keyword tag="http">HTTP</keyword> connections, but for added security you can use <keyword tag="https">HTTPS</keyword> connections, if the host supports it.</p><p/><p><b>Comments:</b></p><p>If the <b>revXMLRPC_SetProtocol</b> <glossary tag="command">command</glossary> encounters an error, the <function tag="result">result</function> is set to an error message beginning with "xmlrpcerr"..</p><p/><p><img src="202653"/> <b>Important!</b> The <b>revXMLRPC_SetProtocol</b> <glossary tag="command">command</glossary> is part of the <glossary tag="XML-RPC library">XML-RPC library</glossary>. To ensure that the <glossary tag="command">command</glossary> works in a <glossary tag="standalone application">standalone application</glossary>, you must include this <glossary tag="LiveCode custom library">custom library</glossary> when you create your <glossary tag="standalone application">standalone</glossary>. In the Inclusions section on the General screen of the <glossary tag="Standalone Application Settings">Standalone Application Settings</glossary> window, make sure "XML-RPC Library" is selected in the list of script libraries.</p>
  </description>
</doc>