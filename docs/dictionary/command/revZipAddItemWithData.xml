<doc>
  <legacy_id>3009</legacy_id>
  <name>revZipAddItemWithData</name>
  <type>command</type>
  <syntax>
    <example>revZipAddItemWithData <i>archivePath</i>, <i>itemName</i>, <i>variableName</i></example>
  </syntax>
  <library>Zip Library</library>
  <objects>
  </objects>
  <synonyms>
  </synonyms>
  <classification>
    <category>Text and Data Processing</category>
  </classification>
  <references>
    <command tag="revZipAddItemWithFile">revZipAddItemWithFile Command</command>
    <command tag="revZipOpenArchive">revZipOpenArchive Command</command>
    <command tag="revZipReplaceItemWithData">revZipReplaceItemWithData Command</command>
    <command tag="revZipAddUncompressedItemWithData">revZipAddUncompressedItemWithData Command</command>
    <command tag="revZipAddUncompressedItemWithFile">revZipAddUncompressedItemWithFile Command</command>
  </references>
  <history>
    <introduced version="2.7.3">Added.</introduced>
  </history>
  <platforms>
    <mac/>
    <windows/>
    <linux/>
    <ios/>
    <android/>
  </platforms>
  <classes>
    <desktop/>
    <server/>
    <web/>
    <mobile/>
  </classes>
  <security>
    <disk access="???"/>
    <network/>
  </security>
  <summary>Adds an item to a zip archive and puts the specified data into it</summary>
  <examples>
    <example>revZipAddItemWithData tArchive, "myZippedItem", "tData"</example>
    <example>revZipAddItemWithData the cArchive of me, "Test.txt", "This is a test text file"</example>
  </examples>
  <description>
    <p>Use the <b>revZipAddItemWithData</b> command to put data from a LiveCode variable into a file in a zip archive. The archive must have first been opened with <b>revZipOpenArchive</b>.  </p><p/><p><b>Parameters:</b></p><p>The <i>archivePath</i> is the absolute path to the zip archive to add the file to. </p><p>The <i>itemName</i> is the unique name of the item to create in the zip archive.  </p><p>The <i>variableName</i> is the name of a variable in your LiveCode program containing data to place into the item in the zip archive. </p><p/><p>If the <b>revZipAddItemWithData</b> command encounters an error then the result will be set to an error code beginning with "ziperr", otherwise the result will be empty.</p>
  </description>
</doc>