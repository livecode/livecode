<doc>
  <legacy_id>3010</legacy_id>
  <name>revZipAddItemWithFile</name>
  <type>command</type>
  <syntax>
    <example>revZipAddItemWithFile <i>archivePath</i>, <i>itemName</i>, <i>filePath</i></example>
  </syntax>
  <library>Zip Library</library>
  <objects>
  </objects>
  <synonyms>
  </synonyms>
  <classification>
    <category>Text and Data Processing</category>
  </classification>
  <references>
    <command tag="revZipAddItemWithData">revZipAddItemWithData Command</command>
  </references>
  <history>
    <introduced version="2.7.3">Added.</introduced>
  </history>
  <platforms>
    <mac/>
    <windows/>
    <linux/>
    <ios/>
    <android/>
  </platforms>
  <classes>
    <desktop/>
    <server/>
    <web/>
    <mobile/>
  </classes>
  <security>
    <disk access="???"/>
    <network/>
  </security>
  <summary>Adds a file to an open zip archive</summary>
  <examples>
    <example>revZipAddItemWithFile tArchive, "myZippedItem", "/myFolder/myZippedItem.rev"</example>
    <example>revZipAddItemWithFile the cArchive of me, tFile, (tFolder  slash  tFile)</example>
  </examples>
  <description>
    <p>Use the <b>revZipAddItemWithFile</b> command to place a file from disk into a currently open zip archive. The archive must have first been opened with <command tag="revZipOpenArchive">revZipOpenArchive</command>. </p><p/><p><b>Parameters:</b></p><p>The <i>archivePath</i> is the absolute path to the zip archive to add the file to.</p><p>The <i>itemName</i> is the unique name of the file in the zip archive. This could for example just be the file name.</p><p>The <i>filePath</i> is the absolute path to the file to add to the archive.</p><p/><p>If the <b>revZipAddItemWithFile</b> command encounters an error then the result will be set to an error code beginning with "ziperr", otherwise the result will be empty.</p>
  </description>
</doc>