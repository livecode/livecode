<doc>
  <legacy_id>3021</legacy_id>
  <name>revZipExtractItemToFile</name>
  <type>command</type>
  <syntax>
    <example>revZipExtractItemToFile <i>archivePath</i>, <i>itemName</i>, <i>fileName</i></example>
  </syntax>
  <library>Zip Library</library>
  <objects>
  </objects>
  <synonyms>
  </synonyms>
  <classification>
    <category>Text and Data Processing</category>
  </classification>
  <references>
    <command tag="revZipExtractItemToVariable">revZipExtractItemToVariable Command</command>
  </references>
  <history>
    <introduced version="2.7.3">Added.</introduced>
  </history>
  <platforms>
    <mac/>
    <windows/>
    <linux/>
    <ios/>
    <android/>
  </platforms>
  <classes>
    <desktop/>
    <server/>
    <web/>
    <mobile/>
  </classes>
  <security>
    <disk access="???"/>
    <network/>
  </security>
  <summary>Extracts an item from a zip archive and places the item in a file.</summary>
  <examples>
    <example>revZipExtractItemToFile tArchive, "myZippedItem", "myFolder/myFile.txt"</example>
  </examples>
  <description>
    <p>Use the <b>revZipExtractItemToFile</b> command to place an item in zip archive into a file on disk. The archive must first have been opened using the <command tag="revZipOpenArchive">revZipOpenArchive </command>command.</p><p/><p><b>Parameters:</b></p><p>The <i>archivePath</i> is the absolute path to the archive to extract from.</p><p>The <i>itemName</i> is the name of the item to extract.</p><p>The <i>fileName</i> is the absolute path to the file to place the extracted data in.</p><p/><p>If the <b>revZipExtractItemToFile</b> command encounters an error then the result will be set to an error code beginning with "ziperr", otherwise the result will be empty.</p>
  </description>
</doc>