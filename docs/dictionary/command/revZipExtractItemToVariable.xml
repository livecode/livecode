<doc>
  <legacy_id>3020</legacy_id>
  <name>revZipExtractItemToVariable</name>
  <type>command</type>
  <syntax>
    <example>revZipExtractItemToVariable <i>archivePath</i>, <i>itemName</i>, <i>variableName</i></example>
  </syntax>
  <library>Zip Library</library>
  <objects>
  </objects>
  <synonyms>
  </synonyms>
  <classification>
    <category>Text and Data Processing</category>
  </classification>
  <references>
    <command tag="revZipExtractItemToFile">revZipExtractItemToFile Command</command>
    <command tag="revZipOpenArchive">revZipOpenArchive Command</command>
    <function tag="revZipEnumerateItems">revZipEnumerateItems Function</function>
  </references>
  <history>
    <introduced version="2.7.3">Added.</introduced>
  </history>
  <platforms>
    <mac/>
    <windows/>
    <linux/>
    <ios/>
    <android/>
  </platforms>
  <classes>
    <desktop/>
    <server/>
    <web/>
    <mobile/>
  </classes>
  <security>
    <disk access="???"/>
    <network/>
  </security>
  <summary>Extracts the data from an item in a zip archive and places it in a variable</summary>
  <examples>
    <example>revZipExtractItemToVariable tArchive, "myZippedItem", "tItemData"</example>
  </examples>
  <description>
    <p>Use the <b>revZipExtractItemToVariable</b> command to load an item from a zip archive into memory as a variable in your LiveCode program. The archive must already have been opened by using the <command tag="revZipOpenArchive">revZipOpenArchive </command>command.</p><p/><p><b>Parameters:</b></p><p>The <i>archivePath</i> is the absolute path to the zip archive to extract from.</p><p>The <i>itemName</i> is the name of the item in the zip archive to extract.</p><p>The <i>variableName</i> is the name of a variable in your LiveCode program to place the extracted data.</p><p/><p>If the <b>revZipExtractItemToVariable</b> command encounters an error, then the result will be set to an error code beginning with "ziperr", otherwise the result will be empty.</p>
  </description>
</doc>