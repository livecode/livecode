<doc>
  <legacy_id>3017</legacy_id>
  <name>revZipRenameItem</name>
  <type>command</type>
  <syntax>
    <example>revZipRenameItem <i>archiveName</i>, <i>itemName</i>, <i>newName</i></example>
  </syntax>
  <library>Zip Library</library>
  <objects>
  </objects>
  <synonyms>
  </synonyms>
  <classification>
    <category>Text and Data Processing</category>
  </classification>
  <references>
    <command tag="revZipDeleteItem">revZipDeleteItem Command</command>
  </references>
  <history>
    <introduced version="2.7.3">Added.</introduced>
  </history>
  <platforms>
    <mac/>
    <windows/>
    <linux/>
    <ios/>
    <android/>
  </platforms>
  <classes>
    <desktop/>
    <server/>
    <web/>
    <mobile/>
  </classes>
  <security>
    <disk access="???"/>
    <network/>
  </security>
  <summary>Renames an item in a zip archive.</summary>
  <examples>
    <example>revZipRenameItem tArchive, "myZippedItem", "myNewZippedItem"</example>
  </examples>
  <description>
    <p>Use the <b>revZipRenameItem</b> command to change the name of an item in an open zip archive. The archive must have first been opened using the <command tag="revZipOpenArchive">revZipOpenArchive</command> command. </p><p/><p><b>Parameters:</b></p><p>The <i>archiveName</i> is the absolute path to the zip archive to process.</p><p>The <i>itemName</i> is the name of the item in the zip archive to rename.</p><p>The <i>newName</i> is the new name to give the item.</p><p/><p>If the <b>revZipRenameItem</b> command encounters an error, then the result will be set to an error code beginning with "ziperr", otherwise the result will be empty.</p>
  </description>
</doc>