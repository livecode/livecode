<doc>
  <legacy_id>3019</legacy_id>
  <name>revZipReplaceItemWithFile</name>
  <type>command</type>
  <syntax>
    <example>revZipReplaceItemWithFile<i>archivePath</i>, <i>itemName</i>, <i>filePath</i></example>
  </syntax>
  <library>Zip Library</library>
  <objects>
  </objects>
  <synonyms>
  </synonyms>
  <classification>
    <category>Text and Data Processing</category>
  </classification>
  <references>
    <command tag="revZipReplaceItemWithData">revZipReplaceItemWithData Command</command>
  </references>
  <history>
    <introduced version="2.7.3">Added.</introduced>
  </history>
  <platforms>
    <mac/>
    <windows/>
    <linux/>
    <ios/>
    <android/>
  </platforms>
  <classes>
    <desktop/>
    <server/>
    <web/>
    <mobile/>
  </classes>
  <security>
    <disk access="???"/>
    <network/>
  </security>
  <summary>Replaces the contents of an item in a zip archive with the contents of a file.</summary>
  <examples>
    <example>revZipReplaceItemWithFile tArchive, "myZippedItem", myFile</example>
    <example>revZipReplaceItemWithFile the cArchive of me, "Test.txt", "Test_New.txt"</example>
  </examples>
  <description>
    <p>Use the <b>revZipReplaceItemWithFile</b> command put the contents of a file on disk into one of the items of an open zip archive. The archive must have first been opened with the <command tag="revZipOpenArchive">revZipOpenArchive command</command>.</p><p/><p><b>Parameters:</b></p><p>The <i>archivePath</i> is the absolute path to the zip archive to process.</p><p>The <i>itemName</i> is the name of the item in the zip archive to replace.</p><p>The <i>filePath</i> is the absolute path to the file whose contents should be placed in the item to replace.</p><p/><p>If the <b>revZipReplaceItemWithFile</b> command encounters an error then the result will be set to an error code beginning with "ziperr", otherwise the result will be empty.</p>
  </description>
</doc>