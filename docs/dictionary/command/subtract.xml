<doc>
  <legacy_id>2003</legacy_id>
  <name>subtract</name>
  <type>command</type>
  <syntax>
    <example>subtract <i>number</i> from [<i>chunk</i> of] <i>container</i></example>
    <example>subtract {<i>number</i> | <i>array</i>} from <i>arrayContainer</i></example>
  </syntax>
  <library></library>
  <objects>
  </objects>
  <synonyms>
  </synonyms>
  <classification>
    <category>Math, Logic, &amp; Dates</category>
  </classification>
  <references>
    <command tag="multiply">multiply Command</command>
    <operator tag="-">- Operator</operator>
  </references>
  <history>
    <introduced version="1.0">Added.</introduced>
    <changed version="1.1"></changed>
  </history>
  <platforms>
    <mac/>
    <windows/>
    <linux/>
    <ios/>
    <android/>
  </platforms>
  <classes>
    <desktop/>
    <server/>
    <web/>
    <mobile/>
  </classes>
  <security>
  </security>
  <summary>Subtracts a number from a <glossary tag="container">container</glossary> and places the resulting <function tag="value">value</function> in the <glossary tag="container">container</glossary>.</summary>
  <examples>
    <example>subtract 15 from oldNumber</example>
    <example>subtract field "Input" from word 2 of field "Output"</example>
    <example>subtract monthlyExpenses from monthlyTotals</example>
  </examples>
  <description>
    <p>Use the <b>subtract</b> <glossary tag="command">command</glossary> to subtract a number from a <glossary tag="container">container</glossary> or a portion of a <glossary tag="container">container</glossary>, or to subtract two <glossary tag="array">arrays</glossary> containing numbers.</p><p/><p><b>Parameters:</b></p><p>The <i>chunk</i> is a <glossary tag="chunk expression">chunk expression</glossary> specifying a portion of the <i>container</i>.</p><p/><p>The <i>container</i> is a <keyword tag="field">field</keyword>, <keyword tag="button">button</keyword>, or <glossary tag="variable">variable</glossary>, or the <keyword tag="message box">message box</keyword>.</p><p/><p>The <i>number</i> is any <glossary tag="expression">expression</glossary> that <glossary tag="evaluate">evaluates</glossary> to a number.</p><p/><p>The <i>arrayContainer</i> is an <glossary tag="array">array</glossary> <glossary tag="variable">variable</glossary> each of whose <glossary tag="element">elements</glossary> is a number.</p><p/><p><b>Comments:</b></p><p>The contents of the <i>container</i> (or the <i>chunk</i> of the <i>container</i>) must be a number or an <glossary tag="expression">expression</glossary> that <glossary tag="evaluate">evaluates</glossary> to a number.</p><p/><p>If a <i>number</i> is subtracted from an <i>arrayContainer</i>, the <i>number</i> is subtracted from each <keyword tag="element">element</keyword>. If an <i>array</i> is subtracted from an <i>arrayContainer</i>, both <glossary tag="array">arrays</glossary> must have the same number of <glossary tag="element">elements</glossary> and the same dimension, and each <keyword tag="element">element</keyword> in the <i>array</i> is subtracted from the corresponding <keyword tag="element">element</keyword> of the <i>arrayContainer</i>.</p><p/><p>If the <i>container</i> or an <keyword tag="element">element</keyword> of the <i>arrayContainer</i> is empty, the <b>subtract</b> <glossary tag="command">command</glossary> treats its contents as zero.</p><p/><p>If <i>container</i> is a <keyword tag="field">field</keyword> or <keyword tag="button">button</keyword>, the <function tag="format">format</function> of the sum is determined by the <property tag="numberFormat">numberFormat</property> <glossary tag="property">property</glossary>.</p><p/><p><b>Changes:</b></p><p>The<code> subtract from <i>arrayContainer</i> </code>form was introduced in version 1.1. In previous versions, only single numbers could be used with the <b>subtract</b> <glossary tag="command">command</glossary>.</p>
  </description>
</doc>