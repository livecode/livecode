<doc>
  <legacy_id>2130</legacy_id>
  <name>topLevel</name>
  <type>command</type>
  <syntax>
    <example>topLevel <i>stack</i></example>
  </syntax>
  <library></library>
  <objects>
  </objects>
  <synonyms>
  </synonyms>
  <classification>
    <category>Windows &amp; Dialog Boxes</category>
  </classification>
  <references>
    <command tag="modeless">modeless Command</command>
    <command tag="choose">choose Command</command>
    <command tag="drawer">drawer Command</command>
    <property tag="cantModify">cantModify Property</property>
  </references>
  <history>
    <introduced version="1.0">Added.</introduced>
  </history>
  <platforms>
    <mac/>
    <windows/>
    <linux/>
  </platforms>
  <classes>
    <desktop/>
    <server/>
    <web/>
  </classes>
  <security>
  </security>
  <summary>Opens a <object tag="stack">stack</object> in an <glossary tag="editable window">editable window</glossary>.</summary>
  <examples>
    <example>
      <p>topLevel stack "Internet Access"</p><p>topLevel stack myOpenStack</p>
    </example>
  </examples>
  <description>
    <p>Use the <b>topLevel</b> <glossary tag="command">command</glossary> to display a <object tag="stack">stack</object> in an <glossary tag="editable window">editable window</glossary>.</p><p/><p><b>Parameters:</b></p><p>The <i>stack</i> is any <href tag="reference/object_reference.rev">stack reference</href>.</p><p/><p><b>Comments:</b></p><p>An editable window can be resized (if its <b>resizable</b> <glossary tag="property">property</glossary> is true), and its <glossary tag="control">controls</glossary> can be <property tag="selected">selected</property>, moved, and changed.  To edit a <object tag="stack">stack</object> of a different style (a <command tag="palette">palette</command>, <command tag="modeless">modeless</command>, or <glossary tag="modal dialog box">modal dialog</glossary>), use the <b>topLevel</b> <glossary tag="command">command</glossary> to display it in an <glossary tag="editable window">editable window</glossary>.</p><p/><p>The <b>topLevel</b> <glossary tag="command">command</glossary> closes the <object tag="stack">stack</object> and reopens it as an <glossary tag="editable window">editable window</glossary>, so <message tag="closeStack">closeStack</message> and <message tag="openStack">openStack</message>, <message tag="closeCard">closeCard</message> and <message tag="openCard">openCard</message>, and (if applicable) <message tag="closeBackground">closeBackground</message> and <message tag="openBackground">openBackground</message> <glossary tag="message">messages</glossary> are sent to the <glossary tag="current card">current card</glossary> as a result of <glossary tag="execute">executing</glossary> this <glossary tag="command">command</glossary>. Use the <command tag="lock messages">lock messages</command> <glossary tag="command">command</glossary> before executing <b>topLevel</b> if you want to prevent these <glossary tag="message">messages</glossary> from being sent.</p><p/><p>If the stack is already in an editable window, the <b>topLevel</b> <glossary tag="command">command</glossary> does not close and reopen it.</p>
  </description>
</doc>