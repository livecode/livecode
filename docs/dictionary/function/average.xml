<doc>
	<legacy_id>1369</legacy_id>
	<name>average</name>
	<type>function</type>

	<syntax>
		<example>average(<i>numbersList</i>)</example>
	</syntax>

	<synonyms>
		<synonym>avg</synonym>
		<synonym>arithmeticMean</synonym>
		<synonym>mean</synonym>
	</synonyms>

	<summary><glossary tag="return">Returns</glossary> the arithmetic mean of a list of numbers.</summary>

	<examples>
<example>average(6,22,8) </example>
<example>put average(importedList) into field "Imported Price"</example>
<example><p>put average(replaceText(field "Values",return,comma)) into avgValue</p></example>
	</examples>

	<history>
		<introduced version="1.0">Added.</introduced>
		<deprecated version=""></deprecated>
		<removed version=""></removed>
		<changed version="1.1">1.1</changed>
		<experimental version=""></experimental>
		<nonexperimental version=""></nonexperimental>
	</history>

	<objects>
	</objects>

	<platforms>
		<mac/>
		<windows/>
		<linux/>
		<ios/>
		<android/>
	</platforms>

	<classes>
		<desktop/>
		<server/>
		<web/>
		<mobile/>
	</classes>

	<security>
	</security>


	<classification>
		<category>Math, Logic, &amp; Dates</category>
	</classification>

	<references>
		<function tag="max">max Function</function>
		<function tag="min">min Function</function>
		<function tag="standardDeviation">standardDeviation Function</function>
		<function tag="median">median Function</function>
		<function tag="statRound">statRound Function</function>
		<function tag="round">round Function</function>
	</references>

	<description>Use the <b>average</b> <control_st tag="function">function</control_st> to find the <function tag="value">value</function> that best represents a group of <glossary tag="value">values</glossary>.<p></p><p><b>Parameters:</b></p><p>The <i>numbersList</i> is a comma-separated list of numbers, or an <glossary tag="expression">expression</glossary> that <glossary tag="evaluate">evaluates</glossary> to such a list, or an <glossary tag="array">array</glossary> containing only numbers.</p><p></p><p><b>Value:</b></p><p>The <b>average</b> function <glossary tag="return">returns</glossary> a number.</p><p></p><p><b>Comments:</b></p><p>The average of a list of numbers is the sum of the items in the list or elements in the array, divided by the number of items or elements.</p><p></p><p>The <b>average</b> <control_st tag="function">function</control_st> can also be written like this:</p><p></p><p></p><p></p><p>If the <i>numbersList</i> is empty, the <b>average</b> <control_st tag="function">function</control_st> returns zero.</p><p></p><p></p><change><p>The ability to use an array was introduced in version 1.1. In previous versions, only lists of numbers could be used with the <b>average</b> <control_st tag="function">function</control_st>.</change></description>
</doc>