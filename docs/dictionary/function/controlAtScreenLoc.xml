<doc>
	<legacy_id></legacy_id>
	<name>controlAtScreenLoc</name>
	<type>function</type>

	<syntax>
		<example>controlAtScreenLoc(loc)</example>
	</syntax>

	<synonyms>
	</synonyms>

	<summary></summary>

	<examples>
<example>put controlAtScreenLoc(100,100)</example>
<example>get the name of (controlAtScreenLoc(the mouseLoc))</example>
	</examples>

	<history>
		<introduced version="6.0">Added.</introduced>
		<deprecated version=""></deprecated>
		<removed version=""></removed>
		
		<experimental version=""></experimental>
		<nonexperimental version=""></nonexperimental>
	</history>

	<objects>
	</objects>

	<platforms>
		<mac/>
		<windows/>
	</platforms>

	<classes>
		<desktop/>
	</classes>

	<security>
	</security>


	<classification>
		<category>User Interaction</category>
	</classification>

	<references>
		<function tag="mouseLoc">mouseLoc Function</function>
		<function tag="controlAtLoc">controlAtLoc Function</function>
		<property tag="location">location Property</property>
		<keyword tag="control">control Keyword</keyword>
	</references>

	<description>
	<overview>Returns the control at the specified location.</overview>

		<parameters>
  <parameter>
    <name>location</name>
    <description>A location, relative to the screen</description>
  </parameter>		</parameters>

	<value>The control at the given location, in the form: control <i>control_number</i></value>
	<comments>The <b>controlAtScreenLoc</b> function returns the control at the given location. The function returns the number of the control, below are examples of getting the name or long id of the control<p></p><p>get the name of (contolAtLoc(<i>location</i>))</p><p>get the long id of (contolAtLoc(<i>location</i>))</p></comments>
	</description>
</doc>