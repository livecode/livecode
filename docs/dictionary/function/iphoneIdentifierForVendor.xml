<doc>
	<legacy_id></legacy_id>
	<name>iphoneIdentifierForVendor</name>
	<type>function</type>

	<syntax>
		<example>iphoneIdentifierForVendor()</example>
	</syntax>

	<synonyms>
	</synonyms>

	<summary>Returns a unique identifier for device. The identifier is the same for all apps that come from the same vendor running of the same device.</summary>

	<examples>
<example>put iphoneIdentifierForVendor() into tSystemID</example>
	</examples>

	<history>
		<introduced version="6.1">Added.</introduced>
		<deprecated version=""></deprecated>
		<removed version=""></removed>
		<changed version=""></changed>
		<experimental version=""></experimental>
		<nonexperimental version=""></nonexperimental>
	</history>

	<objects>
	</objects>

	<platforms>
		<ios/>
	</platforms>

	<classes>
		<mobile/>
	</classes>

	<security>
	</security>


	<classification>
	</classification>

	<references>
		<function tag="iphoneApplicationIdentifier">iphoneApplicationIdentifier Function</function>
	</references>

	<description>
	<overview>Use the <b>iphoneIdentifierForVendor</b> function to find a unique idetifier for a vendor/device combination.</overview>

		<parameters>
		</parameters>

	<value>The <b>iphoneIdentifierForVendor</b> function returns a string.</value>
	<comments>The identifier is the same for all apps that come from the same vendor running on the same device. A different value is returned for apps on the same device that come from different vendors, and for apps on different devices regardless of vendor.<p></p><p>The identifier will remain the same whilst apps from the vendor remain installed on the device. If all apps from the vendor are removed from the device and reinstalled, the value of the identifier will change.</p><p></p><p></p><note>Only available on devices running iOS 6.0 and later.</note></comments>
	</description>
</doc>