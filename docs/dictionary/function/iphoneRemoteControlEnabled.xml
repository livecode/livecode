<doc>
	<legacy_id></legacy_id>
	<name>iphoneRemoteControlEnabled</name>
	<type>function</type>

	<syntax>
		<example>iphoneRemoteControlEnabled</example>
	</syntax>

	<synonyms>
	</synonyms>

	<summary>Queries the current state of remote control access.</summary>

	<examples>
<example>get iphoneRemoteControlEnabled()</example>
	</examples>

	<history>
		<introduced version="6.1">Added</introduced>
		<deprecated version=""></deprecated>
		<removed version=""></removed>
		
		<experimental version=""></experimental>
		<nonexperimental version=""></nonexperimental>
	</history>

	<objects>
	</objects>

	<platforms>
		<ios/>
	</platforms>

	<classes>
		<mobile/>
	</classes>

	<security>
	</security>


	<classification>
	</classification>

	<references>
		<command tag="iphoneDisableRemoteControl">iphoneDisableRemoteControl Command</command>
		<command tag="iphoneSetAudioCategory">iphoneSetAudioCategory Command</command>
		<command tag="iphoneSetRemoteControlDisplay">iphoneSetRemoteControlDisplay command</command>
		<message tag="remoteControlReceived">remoteControlReceived Message</message>
	</references>

	<description>
	<overview>Use the <b>iphoneRemoteControlEnabled</b> funtion to find out if access to the remote control is enabled.</overview>

		<parameters>
		</parameters>

	<value>Returns true is access to the remote control feature is enabled, false if not.</value>
	<comments>The remote control feature on iOS allows access to the audio controls both on an external device and also on the device itself when in lock screen (via double-tapping on the Home button).<p></p><p>Use the <b>iphoneRemoteControlEnabled </b>function to find out if this feature is enabled.</p><p></p><p></p><note>This functionality is only available on iOS 5.x and above.</note></comments>
	</description>
</doc>