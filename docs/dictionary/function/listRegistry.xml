<doc>
	<legacy_id></legacy_id>
	<name>listRegistry</name>
	<type>function</type>

	<syntax>
		<example>listRegistry(<i>parentKey</i>)</example>
	</syntax>

	<synonyms>
	</synonyms>

	<summary>Lists the sub-keys of the specified key in the <glossary tag="registry">Windows system registry</glossary>.</summary>

	<examples>
<example>listRegistry("HKEY_CLASSES_ROOT")</example>
	</examples>

	<history>
		<introduced version="4.5">Added.</introduced>
		<deprecated version=""></deprecated>
		<removed version=""></removed>
		<changed version="6.0">Non-experimental</changed>
		<experimental version=""></experimental>
		<nonexperimental version=""></nonexperimental>
	</history>

	<objects>
	</objects>

	<platforms>
		<windows/>
	</platforms>

	<classes>
		<desktop/>
		<server/>
		<web/>
	</classes>

	<security>
	</security>


	<classification>
		<category>The System Environment</category>
	</classification>

	<references>
		<function tag="setRegistry">setRegistry Function</function>
		<function tag="deleteRegistry">deleteRegistry Function</function>
		<function tag="MCISendString">MCISendString Function</function>
	</references>

	<description>
	<overview>Use the <b>listRegistry</b> <control_st tag="function">function</control_st> to lists the sub-keys of a key in the <glossary tag="registry">Windows system registry</glossary>.</overview>

		<parameters>
  <parameter>
    <name>parentKey</name>
    <description>A key in the Windows registry</description>
  </parameter>		</parameters>

	<value>A return-delimited list of sub-keys.</value>
	<comments>The <b>listRegistry</b> function returns a return-delimited list of sub-keys, i.e. those keys which are direct children of the given parentKey. The specified key should be in the same format as the other registry functions.</comments>
	</description>
</doc>