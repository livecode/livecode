<doc>
  <legacy_id>2015</legacy_id>
  <name>matrixMultiply</name>
  <type>function</type>
  <syntax>
    <example>matrixMultiply(<i>firstArray</i>,<i>secondArray</i>)</example>
  </syntax>
  <library></library>
  <objects>
  </objects>
  <synonyms>
  </synonyms>
  <classification>
    <category>Values &amp; Properties</category>
    <category>Math, Logic, &amp; Dates</category>
  </classification>
  <references>
    <command tag="multiply">multiply Command</command>
    <function tag="transpose">transpose Function</function>
    <function tag="extents">extents Function</function>
  </references>
  <history>
    <introduced version="1.1">Added.</introduced>
  </history>
  <platforms>
    <mac/>
    <windows/>
    <linux/>
    <ios/>
    <android/>
  </platforms>
  <classes>
    <desktop/>
    <server/>
    <web/>
    <mobile/>
  </classes>
  <security>
  </security>
  <summary><glossary tag="return">Returns</glossary> the matrix product of two <glossary tag="array">arrays</glossary>.</summary>
  <examples>
    <example>put matrixMultiply(currentLevels,levelAdjustments)</example>
  </examples>
  <description>
    <p>Use the <b>matrixMultiply</b> <control_st tag="function">function</control_st> to perform matrix multiplication.</p><p/><p><b>Parameters:</b></p><p>The <i>firstArray</i> and <i>secondArray</i> are both two-dimensional <glossary tag="array">array</glossary> <glossary tag="variable">variables</glossary> whose <glossary tag="element">elements</glossary> are numbers, and whose keys are sequential numbers.</p><p/><p><b>Value:</b></p><p>The <b>matrixMultiply</b> <control_st tag="function">function</control_st> <glossary tag="return">returns</glossary> an array of numbers.</p><p/><p><b>Comments:</b></p><p>A two-dimensional array is an array whose elements have a two-part key to describe them. You can visualize such an array as a set of rows and columns: the first part of each element's key is the row number, and the second part is the column number. For example, the expression<code> myArray[3,2] </code>describes the element of<code> myArray </code>which is in the third row, second column.</p><p/><p>The matrix product of two arrays is itself an array. Each element<code> myArray[<i>M</i>,<i>N</i>] </code>of the product is obtained by multiplying each element of the <i>M</i>th row by the corresponding <keyword tag="element">element</keyword> of the <i>N</i>th column. The resulting numbers are added together to obtain the <keyword tag="element">element</keyword> in the <i>M</i>th row and <i>N</i>th column.</p><p/><p>The number of rows in the <i>firstArray</i> must be the same as the number of columns in the <i>secondArray</i>, and the number of rows in the <i>secondArray</i> must match the number of columns in the <i>firstArray</i>. However, the number of rows in the <i>firstArray</i> and <i>secondArray</i> need not be the same.</p>
  </description>
</doc>