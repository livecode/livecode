<doc>
	<legacy_id></legacy_id>
	<name>measureUnicodeText</name>
	<type>function</type>

	<syntax>
		<example>measureUnicodeText(unicodeText,object reference,[mode])</example>
		<example></example>
		<example></example>
	</syntax>

	<synonyms>
	</synonyms>

	<summary>Returns the width, size or bounds of the unicode text drawn with the effective font attribute of the given object.</summary>

	<examples>
<example><p>put measureUnicodeText(theText,me) into theTextWidth</p></example>
<example><p>put measureUnicodeText(theText,me,"size") into theTextSize</p></example>
<example><p>put measureUnicodeText(theText,me,"bounds") into theTextBounds</p></example>
<example><p></p></example>
<example><p></p></example>
	</examples>

	<history>
		<introduced version="6.5">Added.</introduced>
		<deprecated version="7.0">7.0</deprecated>
		<removed version=""></removed>
		
		<experimental version=""></experimental>
		<nonexperimental version=""></nonexperimental>
	</history>

	<objects>
	</objects>

	<platforms>
		<mac/>
		<windows/>
		<linux/>
		<ios/>
		<android/>
	</platforms>

	<classes>
		<desktop/>
		<server/>
		<web/>
		<mobile/>
	</classes>

	<security>
	</security>


	<classification>
		<category>Appearance  Positioning</category>
		<category></category>
		<category></category>
	</classification>

	<references>
		<function tag="measureText">measureText function</function>
		<property tag="formattedWidth">formattedWidth property</property>
		<property tag="formattedHeight">formattedHeight property</property>
	</references>

	<description>
	<overview>Use the <b>measureUnicodeText</b> function to find the dimensions of unicode text drawn with the effective font attributes of an object.</overview>

		<parameters>
		</parameters>

	<value></value>
	<comments><p>Parameters:</p><p>The <i>text</i> is any unicode string. For native strings use measureText.</p><p>The <i>object reference</i> is an expression that evaluates to an object reference.</p><p>The <i>mode</i> is one of:</p><p>width - (default if not specified) - returns the width of the text</p><p>size - returns the width,height of the text</p><p>bounds - returns a rectangle identifying the bounds of the text in the form 0,-ascent,width,descent where ascent and descent are relative to a 0 baseline the text is drawn on.</p><p></p><p>Deprecated: In LiveCode 7.0 the language was changed to handle unicode transparently. This means that language functionality which previously aided unicode text manipulation is no longer required. This function should not be used in new code. measureUnicodeText(tText) is equivalent to measureText(textDecode(tText, "UTF16")).</p></comments>
	</description>
</doc>