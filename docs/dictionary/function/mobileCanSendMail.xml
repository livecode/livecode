<doc>
	<legacy_id></legacy_id>
	<name>mobileCanSendMail</name>
	<type>function</type>

	<syntax>
		<example>mobileCanSendMail ()</example>
	</syntax>

	<synonyms>
		<synonym>iphoneCanSendMail</synonym>
	</synonyms>

	<summary>Determines if a devices can be used to send emails.</summary>

	<examples>
<example>answer mobileCanSendMail () with &quot;okay&quot;</example>
<example><p>if mobileCanSendMail () is false then</p><p>answer &quot;This device does not support e-mail&quot; with &quot;Okay&quot;</p><p>end if</p></example>
	</examples>

	<history>
		<introduced version="4.6">Added.</introduced>
		<deprecated version=""></deprecated>
		<removed version=""></removed>
		<changed version="4.6.1">4.6.1</changed>
	</history>

	<objects>
	</objects>

	<platforms>
		<ios/>
		<android/>
	</platforms>

	<classes>
		<mobile/>
	</classes>

	<security>
	</security>


	<classification>
	</classification>

	<references>
		<command tag="mobileComposeMail">mobileComposeMail Command</command>
		<command tag="mobileComposeUnicodeMail">mobileComposeUnicodeMail Command</command>
		<command tag="mobileComposeHtmlMail">mobileComposeHtmlMail Command</command>
	</references>

	<description>
	<overview>Use the <b>mobileCanSendMail</b> function to determine if a particular mobile device is configured to send email.</overview>

		<parameters>
		</parameters>

	<value>Returns a boolean result:<p>* true - the device supports email submission</p><p>* false - the device does not support email submission</p></value>
	<comments></comments>
	</description>
</doc>