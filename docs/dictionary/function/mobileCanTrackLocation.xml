<doc>
	<legacy_id></legacy_id>
	<name>mobileCanTrackLocation</name>
	<type>function</type>

	<syntax>
		<example>mobileCanTrackLocation()</example>
	</syntax>

	<synonyms>
	</synonyms>

	<summary>Returns true if the device can track the location using GPS, false otherwise.</summary>

	<examples>
<example>get mobileCanTrackLocation()</example>
<example><p>if mobileCanTrackLocation() is &quot;true&quot; then </p><p>  answer &quot;The location can be tracked&quot;</p><p>end if</p></example>
	</examples>

	<history>
		<introduced version="4.6.1">Added</introduced>
		<deprecated version=""></deprecated>
		<removed version=""></removed>
		<changed version="5.5">Android support added</changed>
	</history>

	<objects>
	</objects>

	<platforms>
		<ios/>
		<android/>
	</platforms>

	<classes>
		<mobile/>
	</classes>

	<security>
	</security>


	<classification>
		<category>Sensors</category>
	</classification>

	<references>
		<function tag="mobileSensorReading">mobileSensorReading Function</function>
		<function tag="mobileCurrentLocation">mobileCurrentLocation Function</function>
		<command tag="mobileStartTrackingSensor">mobileStartTrackingSensor Command</command>
		<command tag="mobileStopTrackingSensor">mobileStopTrackingSensor Command</command>
		<message tag="locationChanged">locationChanged Message</message>
		<message tag="trackingError">trackingError Message</message>
	</references>

	<description>
	<overview>Use the <b>mobileCanTrackLocation</b> function to check whether the device has the necessary hardware support for tracking the location using GPS.</overview>

		<parameters>
		</parameters>

	<value>The <b>mobileCanTrackLocation</b> function returns true if the device can track the location and false otherwise.</value>
	<comments></comments>
	</description>
</doc>