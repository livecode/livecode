<doc>
	<legacy_id></legacy_id>
	<name>mobileCurrentLocale</name>
	<type>function</type>

	<syntax>
		<example>mobileCurrentLocale()</example>
	</syntax>

	<synonyms>
		<synonym>iphoneCurrentLocale</synonym>
	</synonyms>

	<summary>Returns the currently configured locale.</summary>

	<examples>
<example>mobileCurrentLocale()</example>
<example><p>if mobileCurrentLocale() is &quot;en_US&quot; then </p><p>  updateDisplay &quot;english&quot;</p><p>end if</p></example>
	</examples>

	<history>
		<introduced version="5.5">Added</introduced>
		<deprecated version=""></deprecated>
		<removed version=""></removed>
		
	</history>

	<objects>
	</objects>

	<platforms>
		<ios/>
		<android/>
	</platforms>

	<classes>
		<mobile/>
	</classes>

	<security>
	</security>


	<classification>
	</classification>

	<references>
		<function tag="mobilePreferredLanguages">mobilePreferredLanguages Function</function>
	</references>

	<description>
	<overview>Use the <b>mobileCurrentLocale</b> function to discover what language the device is currently set to use. This allows you to set up your app for that language.</overview>

		<parameters>
		</parameters>

	<value>The <b>mobileCurrentLocale</b> function returns the currently configured locale tag. For example &quot;en_US&quot;.</value>
	<comments></comments>
	</description>
</doc>