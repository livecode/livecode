<doc>
	<legacy_id></legacy_id>
	<name>mobileNextSoundOnChannel</name>
	<type>function</type>

	<syntax>
		<example>mobileNextSoundOnChannel(<i>channel)</i></example>
	</syntax>

	<synonyms>
		<synonym>iphoneNextSoundOnChannel</synonym>
	</synonyms>

	<summary>Returns the sound that is scheduled to play next (if any) on the given channel.</summary>

	<examples>
<example>get mobileNextSoundOnChannel(theChannel)</example>
<example>put mobileNextSoundOnChannel(&quot;current&quot;) into tNextSound</example>
	</examples>

	<history>
		<introduced version="4.6">Introduced</introduced>
		<deprecated version=""></deprecated>
		<removed version=""></removed>
		<changed version="5.5">Android support added</changed>
	</history>

	<objects>
	</objects>

	<platforms>
		<ios/>
		<android/>
	</platforms>

	<classes>
		<mobile/>
	</classes>

	<security>
	</security>


	<classification>
		<category>sound</category>
	</classification>

	<references>
		<function tag="mobileSoundChannelVolume">mobileSoundChannelVolume Function</function>
		<function tag="mobileSoundOnChannel">mobileSoundOnChannel Function</function>
		<function tag="mobileSoundChannels">mobileSoundChannels Function</function>
		<function tag="mobileSoundChannelStatus">mobileSoundChannelStatus Function</function>
		<command tag="mobilePlaySoundOnChannel">mobilePlaySoundOnChannel Command</command>
		<command tag="mobileStopPlayingOnChannel">mobileStopPlayingOnChannel Command</command>
		<command tag="mobilePausePlayingOnChannel">mobilePausePlayingOnChannel Command</command>
		<command tag="mobileResumePlayingOnChannel">mobileResumePlayingOnChannel Command</command>
		<command tag="mobileSetSoundChannelVolume">mobileSetSoundChannelVolume Command</command>
		<command tag="mobileDeleteSoundChannel">mobileDeleteSoundChannel Command</command>
		<message tag="soundFinishedOnChannel">soundFinishedOnChannel Message</message>
	</references>

	<description>
	<overview>Use the <b>mobileNextSoundOnChannel</b> function to find what sound (if any) is scheduled to play next on the current channel.</overview>

		<parameters>
  <parameter>
    <name>channel</name>
    <description>The name of the channel.</description>
  </parameter>		</parameters>

	<value>The <b>mobileNextSoundOnChannel</b> function returns the name of the sound file that is scheduled to play next on the given channel.<p></p><p>If no sound is scheduled or the channel doesn't exit then empty is returned.</p></value>
	<comments></comments>
	</description>
</doc>