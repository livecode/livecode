<doc>
	<legacy_id></legacy_id>
	<name>mobileOrientationLocked</name>
	<type>function</type>

	<syntax>
		<example>mobileOrientationLocked()</example>
	</syntax>

	<synonyms>
		<synonym>iphoneOrientationLocked</synonym>
	</synonyms>

	<summary>Queries the current lock state.</summary>

	<examples>
<example>answer mobileOrientationLocked() with &quot;Okay&quot;</example>
	</examples>

	<history>
		<introduced version="4.5.3">Added.</introduced>
		<deprecated version=""></deprecated>
		<removed version=""></removed>
		<changed version="4.6.1">4.6.1</changed>
	</history>

	<objects>
	</objects>

	<platforms>
		<ios/>
		<android/>
	</platforms>

	<classes>
		<mobile/>
	</classes>

	<security>
	</security>


	<classification>
	</classification>

	<references>
		<function tag="mobileDeviceOrientation">mobileDeviceOrientation Function</function>
		<function tag="mobileOrientation">mobileOrientation Function</function>
		<function tag="mobileAllowedOrientations">mobileAllowedOrientations Function</function>
		<command tag="mobileSetAllowedOrientations">mobileSetAllowedOrientations Command</command>
		<command tag="mobileUnlockOrientation">mobileUnlockOrientation Command</command>
		<message tag="orientationChanged">orientationChanged Message</message>
	</references>

	<description>
	<overview>Use the <b>mobileOrientationLocked</b> function to query the current orientation lock state.</overview>

		<parameters>
		</parameters>

	<value>The <b>mobileOrientationLocked</b> function returns a boolean value:<p>* true - orientation changes are locked</p><p>* false - orientation changes are note locked</p></value>
	<comments>The <b>mobileOrientationLocked</b> function tests if the current orientation is locked. This may be while your application is performing a task and you do not want the orientation to change until the task is complete.<p></p><p>The orientation lock can be nested, and when an unlock request causes the nesting to return to zero, the interface rotates to match the device's current orientation (assuming it is in the set of allowed orientations).</p></comments>
	</description>
</doc>