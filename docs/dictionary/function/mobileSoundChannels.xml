<doc>
	<legacy_id></legacy_id>
	<name>mobileSoundChannels</name>
	<type>function</type>

	<syntax>
		<example>mobileSoundChannels()</example>
	</syntax>

	<synonyms>
		<synonym>iphoneSoundChannels</synonym>
	</synonyms>

	<summary>Returns a list of the sound channels that currently exist.</summary>

	<examples>
<example>get mobileSoundChannels()</example>
<example>put mobileSoundChannels() into tSoundChannelList</example>
	</examples>

	<history>
		<introduced version="4.6">Introduced</introduced>
		<deprecated version=""></deprecated>
		<removed version=""></removed>
		<changed version="5.5">Android support added</changed>
	</history>

	<objects>
	</objects>

	<platforms>
		<ios/>
		<android/>
	</platforms>

	<classes>
		<mobile/>
	</classes>

	<security>
	</security>


	<classification>
		<category>sound</category>
	</classification>

	<references>
		<function tag="mobileSoundChannelVolume">mobileSoundChannelVolume Function</function>
		<function tag="mobileSoundOnChannel">mobileSoundOnChannel Function</function>
		<function tag="mobileNextSoundOnChannel">mobileNextSoundOnChannel Function</function>
		<function tag="mobileSoundChannelStatus">mobileSoundChannelStatus Function</function>
		<command tag="mobilePlaySoundOnChannel">mobilePlaySoundOnChannel Command</command>
		<command tag="mobileStopPlayingOnChannel">mobileStopPlayingOnChannel Command</command>
		<command tag="mobilePausePlayingOnChannel">mobilePausePlayingOnChannel Command</command>
		<command tag="mobileResumePlayingOnChannel">mobileResumePlayingOnChannel Command</command>
		<command tag="mobileSetSoundChannelVolume">mobileSetSoundChannelVolume Command</command>
		<command tag="mobileDeleteSoundChannel">mobileDeleteSoundChannel Command</command>
		<message tag="soundFinishedOnChannel">soundFinishedOnChannel Message</message>
	</references>

	<description>
	<overview>Use the <b>mobileSoundChannels</b> function to get a list of the sound channels that currently exist.</overview>

		<parameters>
		</parameters>

	<value>The <b>mobileSoundChannels</b> function returns a list of the currently existing channel names, one per line.</value>
	<comments>Sound channels persist after any sounds have finished playing on them, retaining the last volume setting. To remove a channel from memory completely use the <command tag="mobileDeleteSoundChannel">mobileDeleteSoundChannel</command> command.<p></p><p>Sound channels only consume system resources when they are playing sounds, this means you don't need to be concerned about having many around at once (assuming most are inactive).</p></comments>
	</description>
</doc>