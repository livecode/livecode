<doc>
  <legacy_id>1913</legacy_id>
  <name>monthNames</name>
  <type>function</type>
  <syntax>
    <example>the [long | abbr[ev[iated]] | short] [english|system] monthNames</example>
    <example>the [internet] [english | system] monthNames</example>
    <example>monthNames()</example>
  </syntax>
  <library></library>
  <objects>
  </objects>
  <synonyms>
  </synonyms>
  <classification>
    <category>Math, Logic, &amp; Dates</category>
  </classification>
  <references>
    <function tag="dateFormat">dateFormat Function</function>
  </references>
  <history>
    <introduced version="1.1">Added.</introduced>
  </history>
  <platforms>
    <mac/>
    <windows/>
    <linux/>
    <ios/>
    <android/>
  </platforms>
  <classes>
    <desktop/>
    <server/>
    <web/>
    <mobile/>
  </classes>
  <security>
  </security>
  <summary><glossary tag="return">Returns</glossary> a list of month names used by the <function tag="date">date</function> <control_st tag="function">function</control_st>.</summary>
  <examples>
    <example>put the monthNames into listToDelete</example>
    <example>get the long english monthNames</example>
  </examples>
  <description>
    <p>Use the <b>monthNames</b> <control_st tag="function">function</control_st> to let the user choose from a list of month names, or to display a list of months in the language of the user's system preferences.</p><p/><p><b>Value:</b></p><p>The <b>monthNames</b> <control_st tag="function">function</control_st> returns a set of twelve month names, one per line.</p><p/><p>The monthNames form returns the full name of each month, as used in the long date.</p><p/><p>The long monthNames form returns the same result as the monthNames form.</p><p/><p>The abbreviated monthNames form returns the first three letters of the name of each month, as used in the abbreviated date.</p><p/><p>The short monthNames form returns the month number of each month, as used in the short date.</p><p/><p><b>Comments:</b></p><p>For each form of the <b>monthNames</b>, if the <property tag="useSystemDate">useSystemDate</property> <glossary tag="property">property</glossary> is set to true, or if you specify the system monthNames, the list of names is provided in the language specified by the user's system preferences. If the <b>useSystemDate</b> is false or you specify the english monthNames, the list of names is provided in English.</p>
  </description>
</doc>