<doc>
	<legacy_id></legacy_id>
	<name>nativeCharToNum</name>
	<type>function</type>

	<syntax>
		<example>the nativeCharToNum of <i>nativeCharacter</i></example>
		<example>nativeCharToNum(<i>nativeCharacter)</i></example>
	</syntax>

	<synonyms>
	</synonyms>

	<summary>Returns the 8-bit value corresponding to a native character.</summary>

	<examples>
<example>put nativeCharToNum("A")</example>
<example>get nativeCharToNum(space)</example>
	</examples>

	<history>
		<introduced version="7.0">7.0</introduced>
		<deprecated version=""></deprecated>
		<removed version=""></removed>
		<changed version=""></changed>
		<experimental version=""></experimental>
		<nonexperimental version=""></nonexperimental>
	</history>

	<objects>
	</objects>

	<platforms>
		<mac/>
		<windows/>
		<linux/>
		<ios/>
		<android/>
	</platforms>

	<classes>
		<desktop/>
		<server/>
		<web/>
		<mobile/>
	</classes>

	<security>
	</security>


	<classification>
	</classification>

	<references>
		<function tag="numToNativeChar">numToNativeChar function</function>
		<function tag="nativeCharToNum">nativeCharToNum function</function>
		<function tag="codepointToNum">codepointToNum function</function>
	</references>

	<description>
	<overview>Use the <b>nativeCharToNum</b> function to translate native characters to their 8-bit numerical value.</overview>

		<parameters>
  <parameter>
    <name>nativeCharacter</name>
    <description>A single native character.</description>
  </parameter>		</parameters>

	<value>The <b>nativeCharToNum</b> function returns an integer between 0 and 255.</value>
	<comments>The <function tag="nativeCharToNum"><b></function></b></a> function is the inverse of the <function tag="numToNativeChar">numToNativeChar</function> function.<p></p><p>These functions convert between text and native characters and are replacements for the deprecated <function tag="numToChar">numToChar</function> and <function tag="charToNum">charToNum</function> functions.</p><p></p><p>As the native character sets for each platform have a limited and different repertoire, these functions should not be used when preservation of Unicode text is desired. Any characters that cannot be mapped to the native character set are replaced with a question mark character (?).</p><p></p><p>Unless needed for compatibility reasons, it is recommended that you use the <function tag="numToCodepoint">numToCodepoint</function> and <function tag="codepointToNum">codepointToNum</function> functions instead.</p></comments>
	</description>
</doc>