<doc>
	<legacy_id></legacy_id>
	<name>numToByte</name>
	<type>function</type>

	<syntax>
		<example>the numToByte of <i>numericValue</i></example>
		<example>numToByte(<i>numericValue</i>)</example>
	</syntax>

	<synonyms>
	</synonyms>

	<summary>Returns the byte corresponding to a numeric value in the range 0-255.</summary>

	<examples>
<example>numToByte(65) <i>-- returns a byte with numeric value 65</i></example>
<example>numToByte(0) <i>-- returns a byte with numeric value 0</i></example>
	</examples>

	<history>
		<introduced version="4.0">Added.</introduced>
		<deprecated version=""></deprecated>
		<removed version=""></removed>
		<changed version=""></changed>
	</history>

	<objects>
	</objects>

	<platforms>
		<mac/>
		<windows/>
		<linux/>
	  <ios/>
    <android/>
  </platforms>

	<classes>
		<desktop/>
		<server/>
		<web/>
	  <mobile/>
  </classes>

	<security>
	</security>


	<classification>
		<category>Text and Data Processing</category>
	</classification>

	<references>
		<function tag="binaryDecode">binaryDecode Function</function>
		<function tag="binaryEncode">binaryEncode Function</function>
		<function tag="byteToNum">byteToNum Function</function>
		<function tag="charToNum">charToNum Function</function>
		<function tag="ISOToMac">ISOToMac Function</function>
		<function tag="macToISO">macToISO Function</function>
		<function tag="toLower">toLower Function</function>
		<function tag="toUpper">toUpper Function</function>
		<function tag="uniEncode">uniEncode Function</function>
		<function tag="base64Encode">base64Encode Function</function>
		<property tag="charSet">charSet Property</property>
		<property tag="useUnicode">useUnicode Property</property>
		<property tag="HTMLText">HTMLText Property</property>
		<property tag="extendKey">extendKey Property</property>
	</references>

	<description>
	<overview>Use the <b>byteToNum</b> function to translate numbers into their byte equivalents, or to interpret a byte (such as a control character) that can't be displayed.</overview>

		<parameters>
		<parameter>The <i>numericValue</i> is an <keyword tag="integer">integer</keyword> between zero and 255, or an <a name="glossary tag=expression">expression</a> that <glossary tag="evaluate">evaluates</glossary> to such an <keyword tag="integer">integer</keyword>.</parameter>
		</parameters>

	<value>The <b>numToByte</b> function returns a single byte.</value>
	<comments>The <b>numToByte</b> function is the inverse of the <function tag="byteToNum">byteToNum</function> function.</comments>
	</description>
</doc>