<doc>
  <legacy_id>1417</legacy_id>
  <name>openProcesses</name>
  <type>function</type>
  <syntax>
    <example>the openProcesses</example>
    <example>openProcesses()</example>
  </syntax>
  <library></library>
  <objects>
  </objects>
  <synonyms>
  </synonyms>
  <classification>
    <category>The System Environment</category>
  </classification>
  <references>
    <command tag="kill">kill Command</command>
    <command tag="read from process">read from process Command</command>
    <operator tag="there is no">there is no Operator</operator>
  </references>
  <history>
    <introduced version="1.0">Added.</introduced>
  </history>
  <platforms>
    <mac/>
    <windows/>
    <linux/>
  </platforms>
  <classes>
    <desktop/>
    <server/>
    <web/>
  </classes>
  <security>
    <process/>
  </security>
  <summary><glossary tag="return">Returns</glossary> the names of <glossary tag="process">processes</glossary> that have been opened with the <command tag="open process">open process</command> <glossary tag="command">command</glossary>, but have not yet <glossary tag="exit">exited</glossary>.</summary>
  <examples>
    <example>the openProcesses</example>
    <example>put the openProcesses into processesToClose</example>
  </examples>
  <description>
    <p>Use the <b>openProcesses</b> <control_st tag="function">function</control_st> to find out which <glossary tag="process">processes</glossary> need to be closed, or to check whether a <glossary tag="process">process</glossary> is already running before communicating with it.</p><p/><p><b>Value:</b></p><p>The <b>openProcesses</b> <control_st tag="function">function</control_st> <glossary tag="return">returns</glossary> a list of <glossary tag="process">process</glossary> names, one per line.</p><p/><p><b>Comments:</b></p><p>The list of process names is in the same order used by <b>openProcessIDs</b> <control_st tag="function">function</control_st>, which is the same order that the <glossary tag="process">processes</glossary> were opened.</p><p/><p>Programs that were not started by LiveCode are not included in the list returned by the <b>openProcesses</b> <control_st tag="function">function</control_st>.</p>
  </description>
</doc>