<doc>
  <legacy_id>2514</legacy_id>
  <name>pendingMessages</name>
  <type>function</type>
  <syntax>
    <example>the pendingMessages</example>
    <example>pendingMessages()</example>
  </syntax>
  <library></library>
  <objects>
  </objects>
  <synonyms>
  </synonyms>
  <classification>
    <category>Objects &amp; Messages</category>
  </classification>
  <references>
    <command tag="cancel">cancel Command</command>
  </references>
  <history>
    <introduced version="1.0">Added.</introduced>
  </history>
  <platforms>
    <mac/>
    <windows/>
    <linux/>
    <ios/>
    <android/>
  </platforms>
  <classes>
    <desktop/>
    <server/>
    <web/>
    <mobile/>
  </classes>
  <security>
  </security>
  <summary><glossary tag="return">Returns</glossary> a list of <glossary tag="message">messages</glossary> that have been scheduled with the <command tag="send">send</command> <glossary tag="command">command</glossary>, but not yet delivered.</summary>
  <examples>
    <example>the pendingMessages</example>
    <example>repeat with x = 1 to the number of lines of the pendingMessages</example>
  </examples>
  <description>
    <p>Use the <b>pendingMessages</b> <control_st tag="function">function</control_st> to check whether a <keyword tag="message box">message</keyword> has been sent yet, or to perform some action on each pending <keyword tag="message box">message</keyword>.</p><p/><p><b>Value:</b></p><p>The <b>pendingMessages</b> <control_st tag="function">function</control_st> <glossary tag="return">returns</glossary> a list of <glossary tag="message">messages</glossary>, one per line. Each line consists of four <keyword tag="items">items</keyword>, separated by commas:</p><p>        * the message ID</p><p>        * the time the message is scheduled for</p><p>        * the message name</p><p>        * the long <b>ID</b> <glossary tag="property">property</glossary> of the <glossary tag="object">object</glossary> that the <keyword tag="message box">message</keyword> will be sent to</p><p/><p><b>Comments:</b></p><p>The message ID is the same as the one placed in the <b>result</b> function when it was scheduled with the <command tag="send">send</command> command.</p><p/><p>The time the message is scheduled for is in the same format as the<code> long seconds </code>form of the <b>seconds</b> <control_st tag="function">function</control_st>.</p><p/><p>Once scheduled, a message cannot be changed. You can cancel a pending message with the <b>cancel</b> <glossary tag="command">command</glossary>, and re-send it with the <command tag="send">send</command> <glossary tag="command">command</glossary>.</p>
  </description>
</doc>