<doc>
  <legacy_id>3069</legacy_id>
  <name>revBrowserInstances</name>
  <type>function</type>
  <syntax>
    <example>revBrowserInstances()</example>
  </syntax>
  <library>Browser Library</library>
  <objects>
  </objects>
  <synonyms>
  </synonyms>
  <classification>
  </classification>
  <references>
    <command tag="revBrowserSet">revBrowserSet Command</command>
    <command tag="revBrowserClose">revBrowserClose Command</command>
  </references>
  <history>
    <introduced version="2.8.1">Added.</introduced>
  </history>
  <platforms>
    <mac/>
    <windows/>
  </platforms>
  <classes>
    <desktop/>
  </classes>
  <security>
    <network/>
  </security>
  <summary>Returns a list of the open browser instances.</summary>
  <examples>
    <example>local tInstances, tInstance</p><p>put revBrowserInstances() into tInstances</p><p>repeat for each item tInstance in tInstances</p><p>  revBrowserClose tInstance</p><p>end repeat</example>
  </examples>
  <description>
    <p>Use the <b>revBrowserInstances</b> function to obtain a list of the ids of all the open browser objects. </p><p/><p><b>Parameters:</b></p><p>none</p><p/><p><b>Value:</b></p><p>The <b>revBrowserInstances</b> function returns a comma-delimited list of integer browser ids, or empty if no browsers are open.</p><p/><p><b>Note:</b> For general information on using the browser library, see the notes in the <function tag="revBrowserOpen">revBrowserOpen function</function> reference.</p>
  </description>
</doc>