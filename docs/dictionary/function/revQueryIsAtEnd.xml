<doc>
  <legacy_id>3074</legacy_id>
  <name>revQueryIsAtEnd</name>
  <type>function</type>
  <syntax>
    <example>revQueryIsAtEnd(<i>recordSetID</i>)</example>
  </syntax>
  <library>Database Library</library>
  <objects>
  </objects>
  <synonyms>
    <synonym>revdb_iseof</synonym>
  </synonyms>
  <classification>
  </classification>
  <references>
    <command tag="revMoveToNextRecord">revMoveToNextRecord Command</command>
    <function tag="revQueryIsAtStart">revQueryIsAtStart Function</function>
  </references>
  <history>
    <introduced version="2.9">Added.</introduced>
  </history>
  <platforms>
    <mac/>
    <windows/>
    <linux/>
    <ios/>
    <android/>
  </platforms>
  <classes>
    <desktop/>
    <server/>
    <web/>
    <mobile/>
  </classes>
  <security>
    <disk access="???"/>
    <network/>
  </security>
  <summary>Returns whether the end of a record set has been reached or not.</summary>
  <examples>
    <example>repeat until revQueryIsAtEnd(tCursor)</p><p>  ...</p><p>  revMoveToNextRecord tCursor</p><p>end repeat</example>
  </examples>
  <description>
    <p>Use the <b>revQueryIsAtEnd</b> function to stop when you reach the end of a record set.</p><p/><p><b>Parameters:</b></p><p>The <i>recordSetID</i> is the number returned by the <function tag="revQueryDatabase">revQueryDatabase function</function> when the record set was created</p><p/><p><b>Value:</b></p><p>The <b>revQueryIsAtEnd</b> function returns true if the <command tag="revMoveToNextRecord">revMoveToNextRecord command</command> has been called with the cursor pointing to the last record.</p><p/><p><b>Comments:</b></p><p>If the operation is not successful, the <b>revQueryIsAtEnd</b> function returns an error message that begins with the string "revdberr"</p><p/>
  </description>
</doc>