<doc>
  <legacy_id>1881</legacy_id>
  <name>selectedImage</name>
  <type>function</type>
  <syntax>
    <example>the selectedImage</example>
    <example>selectedImage()</example>
  </syntax>
  <library></library>
  <objects>
  </objects>
  <synonyms>
  </synonyms>
  <classification>
    <category>User Interaction</category>
    <category>Images &amp; Multimedia</category>
  </classification>
  <references>
    <command tag="choose">choose Command</command>
    <property tag="selected">selected Property</property>
  </references>
  <history>
    <introduced version="2.0">Added.</introduced>
  </history>
  <platforms>
    <mac/>
    <windows/>
    <linux/>
    <ios/>
    <android/>
  </platforms>
  <classes>
    <desktop/>
    <server/>
    <web/>
    <mobile/>
  </classes>
  <security>
  </security>
  <summary>Returns the number of the <keyword tag="image">image</keyword> which is partly or completely <property tag="selected">selected</property> with the Select tool.</summary>
  <examples>
    <example>the selectedImage</example>
    <example>put the short name of the selectedImage into field "Current Image"</example>
  </examples>
  <description>
    <p>Use the <b>selectedImage</b> <control_st tag="function">function</control_st> to find out which <keyword tag="image">image</keyword> the user is painting in.</p><p/><p><b>Value:</b></p><p>The <b>selectedImage</b> <control_st tag="function">function</control_st> returns the word "image" followed by an <glossary tag="image">image's</glossary> <function tag="number">number</function>.</p><p/><p>If nothing is selected with the select tool, the <b>selectedImage</b> <control_st tag="function">function</control_st> returns empty.</p><p/><p><b>Comments:</b></p><p>You use the Pointer tool to select entire objects (including images), but you use the Select tool (which is one of the paint tools) to select a rectangular portion of the picture that's contained in an image. The <b>selectedImage</b> <control_st tag="function">function</control_st> reports on the Select tool's selection, not the <glossary tag="Pointer tool">Pointer tool's</glossary>.</p><p/><p>To find out which objects are selected with the Pointer tool, use the <b>selectedObject</b> <control_st tag="function">function</control_st> instead.</p>
  </description>
</doc>