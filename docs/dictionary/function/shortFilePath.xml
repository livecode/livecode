<doc>
  <legacy_id>1434</legacy_id>
  <name>shortFilePath</name>
  <type>function</type>
  <syntax>
    <example>the shortFilePath of <i>filePath</i></example>
    <example>shortFilePath(<i>filePath</i>)</example>
  </syntax>
  <library></library>
  <objects>
  </objects>
  <synonyms>
  </synonyms>
  <classification>
    <category>Files, Folders, &amp; Resources</category>
  </classification>
  <references>
    <function tag="longFilePath">longFilePath Function</function>
    <property tag="filename">filename Property</property>
  </references>
  <history>
    <introduced version="1.1">Added.</introduced>
  </history>
  <platforms>
    <mac/>
    <windows/>
    <linux/>
  </platforms>
  <classes>
    <desktop/>
    <server/>
    <web/>
  </classes>
  <security>
    <disk access="???"/>
  </security>
  <summary><glossary tag="return">Returns</glossary> the 8.3-format equivalent of a <function tag="openStacks">Windows</function> long <glossary tag="file path">file path</glossary>.</summary>
  <examples>
    <example>the shortFilePath of myPath</example>
    <example>if it is the shortFilePath of it then put it into myFilePath</example>
  </examples>
  <description>
    <p>Use the <b>shortFilePath</b> <control_st tag="function">function</control_st> to obtain the equivalent of a file path in short format for use in situations that require it.</p><p/><p><b>Parameters:</b></p><p>The <i>filePath</i> is the location and name of the <keyword tag="file">file</keyword> or <property tag="defaultFolder">folder</property> whose short equivalent you want. If you specify a name but not a location, LiveCode assumes the <keyword tag="file">file</keyword> or <property tag="defaultFolder">folder</property> is in the <property tag="defaultFolder">defaultFolder</property>.</p><p/><p><b>Value:</b></p><p>The <b>shortFilePath</b> <control_st tag="function">function</control_st> <glossary tag="return">returns</glossary> a <glossary tag="file path">file path</glossary>.</p><p/><p><b>Comments:</b></p><p>The <b>shortFilePath</b> <control_st tag="function">function</control_st> transforms the <i>filePath</i> without looking for the <keyword tag="file">file</keyword>, so it <glossary tag="return">returns</glossary> a value even if the <i>filePath</i> does not exist.</p><p/><p>On Mac OS, OS X, or Unix systems, the <b>shortFilePath</b> simply returns the <i>filePath</i> without changing it.</p>
  </description>
</doc>