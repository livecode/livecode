<doc>
  <legacy_id>2078</legacy_id>
  <name>stacks</name>
  <type>function</type>
  <syntax>
    <example>the stacks</example>
    <example>stacks()</example>
  </syntax>
  <library></library>
  <objects>
  </objects>
  <synonyms>
  </synonyms>
  <classification>
    <category>Objects &amp; Messages</category>
    <category>Files, Folders, &amp; Resources</category>
  </classification>
  <references>
    <function tag="mainStacks">mainStacks Function</function>
  </references>
  <history>
    <introduced version="1.0">Added.</introduced>
  </history>
  <platforms>
    <mac/>
    <windows/>
    <linux/>
    <ios/>
    <android/>
  </platforms>
  <classes>
    <desktop/>
    <server/>
    <web/>
    <mobile/>
  </classes>
  <security>
  </security>
  <summary><glossary tag="return">Returns</glossary> a list of <glossary tag="stack file">stack files</glossary> that contain an open <object tag="stack">stack</object>.</summary>
  <examples>
    <example>the stacks</example>
  </examples>
  <description>
    <p>Use the <b>stacks</b> <control_st tag="function">function</control_st> to find out which <glossary tag="stack file">stack files</glossary> are open.</p><p/><p><b>Value:</b></p><p>The <b>stacks</b> function <glossary tag="return">returns</glossary> a list of <glossary tag="file path">file paths</glossary> of open <function tag="stacks">stacks</function>, one per <keyword tag="line">line</keyword>.</p><p/><p><b>Comments:</b></p><p>Since unsaved stacks have no file name, the file names of unsaved stacks are not included in the list. Instead, each unsaved stack is represented by a blank line in the list.</p><p/><p>The file paths of substacks are included, but the stack names themselves are not. This means that if more than one stack in a file is open, that file's path appears more than once in the list returned by the <b>stacks</b> <control_st tag="function">function</control_st>.</p><p/><p>The list includes any open LiveCode windows (such as the message box and menu bar). It also includes stacks that have been opened during the current session and then closed, unless the stack's <b>destroyStack</b> <glossary tag="property">property</glossary> is set to true.</p>
  </description>
</doc>