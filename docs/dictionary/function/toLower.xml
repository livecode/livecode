<doc>
	<legacy_id>1958</legacy_id>
	<name>toLower</name>
	<type>function</type>

	<syntax>
		<example>the toLower of <i>stringToConvert</i></example>
		<example>toLower(<i>stringToConvert</i>)</example>
	</syntax>

	<synonyms>
		<synonym>lower</synonym>
	</synonyms>

	<summary><glossary tag="return">Returns</glossary> a <keyword tag="string">string</keyword> converted to all lowercase letters. </summary>

	<examples>
<example>toLower(&quot;This is a 30 test.&quot;) <i>-- returns &quot;this is a 30 test.&quot;</i></example>
<example>toLower(&quot;NO, MAANA!&quot;) <i>-- returns &quot;no, maana!&quot;</i></example>
	</examples>

	<history>
		<introduced version="1.0">Added. </introduced>
		<deprecated version=""></deprecated>
		<removed version=""></removed>
		
	</history>

	<objects>
	</objects>

	<platforms>
		<mac/>
		<windows/>
		<linux/>
	  <ios/>
    <android/>
  </platforms>

	<classes>
		<desktop/>
		<server/>
		<web/>
	  <mobile/>
  </classes>

	<security>
	</security>


	<classification>
		<category>Text and Data Processing</category>
	</classification>

	<references>
		<function tag="numToChar">numToChar Function</function>
		<function tag="toUpper">toUpper Function</function>
	</references>

	<description>
	<overview>Use the <b>toLower</b> <control_st tag="function">function</control_st> to change the case of a <keyword tag="string">string</keyword>.</overview>

		<parameters>
		<parameter>The <i>stringToConvert</i> is any string or expression that evaluates to a string.</parameter>
		</parameters>

	<value>The <b>toLower</b> <control_st tag="function">function</control_st> <glossary tag="return">returns</glossary> a <keyword tag="string">string</keyword> the same length as the <i>stringToConvert</i>.</value>
	<comments>Uppercase letters, including special characters with diacritical marks, are converted to the lowercase equivalents. All other characters, including lowercase letters, numbers, punctuation, and special characters with no upper or lower case, are left unchanged by the <b>toLower</b> <control_st tag="function">function</control_st>. </comments>
	</description>
</doc>