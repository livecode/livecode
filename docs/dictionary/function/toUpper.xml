<doc>
	<legacy_id>2285</legacy_id>
	<name>toUpper</name>
	<type>function</type>

	<syntax>
		<example>the toUpper of <i>stringToConvert</i></example>
		<example>toUpper(<i>stringToConvert</i>)</example>
	</syntax>

	<synonyms>
		<synonym>upper</synonym>
	</synonyms>

	<summary><glossary tag="return">Returns</glossary> a <keyword tag="string">string</keyword> converted to all uppercase letters.</summary>

	<examples>
<example>toUpper(&quot;AbC; dEf&quot;) <i>-- returns &quot;ABC; DEF&quot;</i></example>
<example>toUpper(&quot;eclairs are VERY tasty!&quot;) <i>-- returns &quot;ECLAIRS ARE VERY TASTY!&quot;</i></example>
	</examples>

	<history>
		<introduced version="1.0">Added.</introduced>
		<deprecated version=""></deprecated>
		<removed version=""></removed>
		
	</history>

	<objects>
	</objects>

	<platforms>
		<mac/>
		<windows/>
		<linux/>
	  <ios/>
    <android/>
  </platforms>

	<classes>
		<desktop/>
		<server/>
		<web/>
	  <mobile/>
  </classes>

	<security>
	</security>


	<classification>
		<category>Text and Data Processing</category>
	</classification>

	<references>
		<function tag="numToChar">numToChar Function</function>
		<function tag="toLower">toLower Function</function>
	</references>

	<description><overview>Use the <b>toUpper</b> <control_st tag="function">function</control_st> to change the case of a <keyword tag="string">string</keyword>.</overview><p></p><p><b>Parameters:</b></p><p>The <i>stringToConvert</i> is any <keyword tag="string">string</keyword> or <glossary tag="expression">expression</glossary> that <glossary tag="evaluate">evaluates</glossary> to a <keyword tag="string">string</keyword>.</p><p></p><p><b>Value:</b></p><p>The <b>toUpper</b> <control_st tag="function">function</control_st> <glossary tag="return">returns</glossary> a <keyword tag="string">string</keyword> the same length as the <i>stringToConvert</i>.</p><p></p><p><b>Comments:</b></p><p>Lowercase letters, including special characters with diacritical marks, are converted to the uppercase equivalents. All other characters, including uppercase letters, numbers, punctuation, and special characters with no upper or lower case, are left unchanged by the <b>toUpper</b> <control_st tag="function">function</control_st>.</p></description>
</doc>