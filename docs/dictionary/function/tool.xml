<doc>
  <legacy_id>2518</legacy_id>
  <name>tool</name>
  <type>function</type>
  <syntax>
    <example>the tool</example>
    <example>tool()</example>
  </syntax>
  <library></library>
  <objects>
  </objects>
  <synonyms>
  </synonyms>
  <classification>
    <category>Developing with LiveCode</category>
  </classification>
  <references>
    <command tag="choose">choose Command</command>
  </references>
  <history>
    <introduced version="1.0">Added.</introduced>
  </history>
  <platforms>
    <mac/>
    <windows/>
    <linux/>
    <ios/>
    <android/>
  </platforms>
  <classes>
    <desktop/>
    <web/>
    <mobile/>
  </classes>
  <security>
  </security>
  <summary><glossary tag="return">Returns</glossary> the name of the currently chosen <function tag="tool">tool</function>.</summary>
  <examples>
    <example>the tool</example>
    <example>if the tool is not "browse tool" then choose browse tool</example>
  </examples>
  <description>
    <p>Use the <b>tool</b> <control_st tag="function">function</control_st> to find out which <function tag="tool">tool</function> is currently selected.</p><p/><p><b>Value:</b></p><p>The <b>tool</b> <control_st tag="function">function</control_st> <glossary tag="return">returns</glossary> one of the following <function tag="tool">tool</function> names:</p><p/><p><code>browse</code>,<code> pointer</code>,<code> button</code>,<code> field</code>,<code> scrollbar</code>,<code> graphic</code>,<code> image</code>,<code> player</code>,<code> select</code>,<code> pencil</code>,<code> bucket</code>,<code> brush</code>,<code> eraser</code>,<code> spray can</code>,<code> rectangle</code>,<code> line</code>,<code> round rect</code>,<code> oval</code>,<code> polygon</code>,<code> curve</code>,<code> regular polygon</code>, <code>dropper</code></p><p/><p>followed by the word "<code>tool</code>".</p><p/><p><b>Comments:</b></p><p>The tool names are the same as the names used with the <b>choose</b> <glossary tag="command">command</glossary> to select a <function tag="tool">tool</function> for use.</p>
  </description>
</doc>