<doc>
	<legacy_id></legacy_id>
	<name>urlResponse</name>
	<type>function</type>

	<syntax>
		<example>the urlResponse</example>
	</syntax>

	<synonyms>
	</synonyms>

	<summary>Returns the HTTP server response for a post, get, delete or put url operation.</summary>

	<examples>
<example>get the urlResponse</example>
<example>put the urlResponse into tURLContent</example>
	</examples>

	<history>
		<introduced version="3.5">Added.</introduced>
		<deprecated version=""></deprecated>
		<removed version=""></removed>
		
	</history>

	<objects>
	</objects>

	<platforms>
		<mac/>
		<windows/>
		<linux/>
	</platforms>

	<classes>
		<desktop/>
		<server/>
		<web/>
	</classes>

	<security>
	</security>


	<classification>
	</classification>

	<references>
		<function tag="result">result Function</function>
		<command tag="post">post Command</command>
		<command tag="delete URL">delete URL Command</command>
		<command tag="put">put Command</command>
		<command tag="get">get Command</command>
		<keyword tag="URL">URL Keyword</keyword>
	</references>

	<description>
	<overview>Use the urlResponse function to check the data returned by the server after an HTTP request.</overview>

		<parameters>
		</parameters>

	<value>The urlResponse function returns a string.</value>
	<comments>All HTTP requests cause data to be sent to the server and data to be returned from the server. The urlResponse function allows you to access any data returned by the HTTP server in the last URL operation, the urlResonse function returns empty if the operation failed.<p></p><p>The HTTP requests which cause data to be returned by the urlResponse function are <command tag="get">get</command>, <command tag="put">put</command>, <command tag="delete">delete</command> and <command tag="post">post</command>.</p></comments>
	</description>
</doc>