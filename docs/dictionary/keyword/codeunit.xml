<doc>
	<legacy_id></legacy_id>
	<name>codeunit</name>
	<type>keyword</type>

	<syntax>
	</syntax>

	<synonyms>
	</synonyms>

	<summary>Designates the encoded unit in which a codepoint is stored.</summary>

	<examples>
<example>get codeunit 1 of tString</example>
<example>get the first codeunit of tString</example>
	</examples>

	<history>
		<introduced version="7.0">Added.</introduced>
		<deprecated version=""></deprecated>
		<removed version=""></removed>
		<changed version=""></changed>
		<experimental version=""></experimental>
		<nonexperimental version=""></nonexperimental>
	</history>

	<objects>
	</objects>

	<platforms>
		<mac/>
		<windows/>
		<linux/>
		<ios/>
		<android/>
	</platforms>

	<classes>
		<desktop/>
		<server/>
		<web/>
		<mobile/>
	</classes>

	<security>
	</security>


	<classification>
	</classification>

	<references>
		<keyword tag="character">character Keyword</keyword>
		<keyword tag="characters">characters Keyword</keyword>
		<keyword tag="byte">byte Keyword</keyword>
		<keyword tag="bytes">bytes Keyword</keyword>
		<keyword tag="codepoint">codepoints Keyword</keyword>
		<keyword tag="codepoints">codepoints Keyword</keyword>
		<keyword tag="codeunits">codeunits Keyword</keyword>
	</references>

	<description>
	<overview>Use the <b>codeunit</b> <glossary tag="keyword">keyword</glossary> to refer to the encoded units in which codepoints are stored.</overview>

		<parameters>
		</parameters>

	<value></value>
	<comments>A <b>codeunit</b> is the encoded units in which codepoints are stored (e.g. 16 bit words in UTF-16 or 8-bit bytes in UTF-8). Although the engine currently uses UTF-16 for storage of Unicode text, this should not be relied upon. The byte order of the codeunits depends on the platform and it is not guaranteed that the engine will always use UTF-16 for text storage. <p></p><p>The hierarchy of the new and altered chunk types is as follows:</p><p></p><p>byte w of codeunit x of codepoint y of char z of word ...</p><p></p><p></p><note>This chunk type is not of general utility.</note></comments>
	</description>
</doc>