<doc>
  <legacy_id>2265</legacy_id>
  <name>dateItems</name>
  <type>keyword</type>
  <syntax>
  </syntax>
  <library></library>
  <objects>
  </objects>
  <synonyms>
  </synonyms>
  <classification>
    <category>Math, Logic, &amp; Dates</category>
  </classification>
  <references>
    <command tag="convert">convert Command</command>
  </references>
  <history>
    <introduced version="1.0">Added.</introduced>
  </history>
  <platforms>
    <mac/>
    <windows/>
    <linux/>
    <ios/>
    <android/>
  </platforms>
  <classes>
    <desktop/>
    <server/>
    <web/>
    <mobile/>
  </classes>
  <security>
  </security>
  <summary>Designates a comma-delimited date format used with the <command tag="convert">convert</command> <glossary tag="command">command</glossary>.</summary>
  <examples>
    <example>convert myVariable to dateItems</example>
    <example>convert it from dateItems to long date and long time</example>
  </examples>
  <description>
    <p>Use the <b>dateItems</b> <glossary tag="keyword">keyword</glossary> with the <command tag="convert">convert</command> <glossary tag="command">command</glossary> to store a date and/or time.</p><p/><p><b>Comments:</b></p><p>The <b>dateItems</b> format is a comma-separated list of numbers:</p><p>        * the year</p><p>        * the month number</p><p>        * the day of the month</p><p>        * the hour in 24-hour time</p><p>        * the minute</p><p>        * the second</p><p>        * the numeric day of the week where Monday is day 1, Tuesday is day 2, and so forth</p><p/><p>The <b>dateItems</b> does not change depending on the user's settings, so you can use it (or  the <function tag="seconds">seconds</function> <function tag="format">format</function>) to store a date and time with a <object tag="stack">stack</object>, in an invariant form that won't change.</p><p/><p/><p/>
  </description>
</doc>