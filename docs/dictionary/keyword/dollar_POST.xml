<doc>
	<legacy_id></legacy_id>
	<name>$_POST</name>
	<type>keyword</type>

	<syntax>
	</syntax>

	<synonyms>
	</synonyms>

	<summary><b>$_POST</b> is an array variable, formed from reading stdin and translating the url-form-encoded or multi-part-encoded data.</summary>

	<examples>
<example>put $_POST into tPOSTArray</example>
<example>put $_POST[&quot;name&quot;] into tName</example>
	</examples>

	<history>
		<introduced version="4.6.3">Introduced</introduced>
		<deprecated version=""></deprecated>
		<removed version=""></removed>
		<changed version="4.6.4">Added support for multi-part-encoding</changed>
	</history>

	<objects>
	</objects>

	<platforms>
		<mac/>
		<windows/>
		<linux/>
	</platforms>

	<classes>
		<server/>
	</classes>

	<security>
	</security>


	<classification>
	</classification>

	<references>
		<property tag="outputTextEncoding">outputTextEncoding Property</property>
		<keyword tag="$_SERVER">$_SERVER Keyword</keyword>
		<keyword tag="$_GET">$_GET Keyword</keyword>
		<keyword tag="$_GET_BINARY">$_GET_BINARY Keyword</keyword>
		<keyword tag="$_GET_RAW">$_GET_RAW Keyword</keyword>
		<keyword tag="$_POST_BINARY">$_POST_BINARY keyword</keyword>
		<keyword tag="$_POST_RAW">$_POST_RAW keyword</keyword>
	</references>

	<description>
	<overview>Use the <b>$_POST</b> keyword to access posted form data. It is an array formed from reading stdin and translating the data. <p></p><p><b>$_POST</b> is only available when running in CGI mode (Server).</p></overview>

		<parameters>
		</parameters>

	<value></value>
	<comments>The data is converted to the native character set from the character set defined in the <property tag="outputTextEncoding">outputTextEncoding</property>.<p>If the data is not url-form-encoded or multi-part-encoded then the result is empty. </p><p>The <b>$_POST</b> keyword supports multi dimensional arrays.</p></comments>
	</description>
</doc>