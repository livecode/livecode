<doc>
  <legacy_id>1238</legacy_id>
  <name>field</name>
  <type>keyword</type>
  <syntax>
  </syntax>
  <library></library>
  <objects>
  </objects>
  <synonyms>
  </synonyms>
  <classification>
    <category>Developing with LiveCode</category>
  </classification>
  <references>
    <command tag="choose">choose Command</command>
  </references>
  <history>
    <introduced version="1.0">Added.</introduced>
  </history>
  <platforms>
    <mac/>
    <windows/>
    <linux/>
    <ios/>
    <android/>
  </platforms>
  <classes>
    <desktop/>
    <server/>
    <web/>
    <mobile/>
  </classes>
  <security>
  </security>
  <summary>Designates the Field <function tag="tool">tool</function>, which is used to create new <glossary tag="field">fields</glossary>.</summary>
  <examples>
    <example>choose field tool</example>
  </examples>
  <description>
    <p>Use the <b>field</b> <glossary tag="keyword">keyword</glossary> to create fields.</p><p/><p><b>Comments:</b></p><p>When using the Field tool, the cursor is a crosshairs. This tool is used for creating fields by clicking at one corner of the field and dragging to the opposite corner.</p><p/><p>You can set the <b>style</b> (and other <property tag="properties">properties</property>) of the <keyword tag="templateField">templateField</keyword> to the <keyword tag="field">field</keyword> type you want, then draw the <keyword tag="field">field</keyword> with the Field <function tag="tool">tool</function>. The <keyword tag="field">field</keyword> icons on the Tools <command tag="palette">palette</command> work this way: each one sets the <property tag="style">style</property> of the <keyword tag="templateField">templateField</keyword>, then chooses the Field <function tag="tool">tool</function> so you can create the <keyword tag="field">field</keyword>.</p>
  </description>
</doc>