<doc>
  <legacy_id>2503</legacy_id>
  <name>integer</name>
  <type>keyword</type>
  <syntax>
  </syntax>
  <library></library>
  <objects>
  </objects>
  <synonyms>
  </synonyms>
  <classification>
    <category>Values &amp; Properties</category>
  </classification>
  <references>
    <operator tag="is a">is a Operator</operator>
    <operator tag="is not a">is not a Operator</operator>
  </references>
  <history>
    <introduced version="1.0">Added.</introduced>
  </history>
  <platforms>
    <mac/>
    <windows/>
    <linux/>
    <ios/>
    <android/>
  </platforms>
  <classes>
    <desktop/>
    <server/>
    <web/>
    <mobile/>
  </classes>
  <security>
  </security>
  <summary>Used with the <operator tag="is a">is a</operator> and <operator tag="is not a">is not a</operator> <glossary tag="operator">operators</glossary> to check whether a value is an <keyword tag="integer">integer</keyword>.</summary>
  <examples>
    <example>if field "Number to Order" is not an integer then beep</example>
  </examples>
  <description>
    <p>Use the <b>integer</b> <glossary tag="keyword">keyword</glossary> to find out whether a value is a number that doesn't have a fractional part.</p><p/><p><b>Comments:</b></p><p>If the value contains digits (and an optional minus sign) and no other characters, it is an integer.</p><p/><p>If the value is an expression, it is evaluated and LiveCode checks whether the final value is an integer. For example, the value of the expression "22 - 3" is 19 (which is an integer), so the following expression evaluates to true:</p><p/><p><code>  22 - 3 is an integer</code></p><p/><p>However, the expression</p><p><code>  "22 - 3" is an integer</code></p><p>evaluates to false, because the double quotes prevent the expression "22 - 3" from being evaluated.</p>
  </description>
</doc>