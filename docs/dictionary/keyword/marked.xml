<doc>
  <legacy_id>1745</legacy_id>
  <name>marked</name>
  <type>keyword</type>
  <syntax>
  </syntax>
  <library></library>
  <objects>
  </objects>
  <synonyms>
  </synonyms>
  <classification>
    <category>Appearance &amp; Positioning</category>
  </classification>
  <references>
    <command tag="mark">mark Command</command>
    <property tag="mark">mark Property</property>
    <command tag="unmark">unmark Command</command>
  </references>
  <history>
    <introduced version="1.0">Added.</introduced>
  </history>
  <platforms>
    <mac/>
    <windows/>
    <linux/>
    <ios/>
    <android/>
  </platforms>
  <classes>
    <desktop/>
    <server/>
    <web/>
    <mobile/>
  </classes>
  <security>
  </security>
  <summary>Used in card references and by the <command tag="sort">sort</command> and <command tag="print">print</command> <glossary tag="command">commands</glossary>.</summary>
  <examples>
    <example>go to marked card 3</example>
    <example>get the short name of next marked card</example>
    <example>sort marked cards descending by field "Name"</example>
    <example>print marked cards</example>
  </examples>
  <description>
    <p>Use the <b>marked</b> <glossary tag="keyword">keyword</glossary> to refer to <glossary tag="card">cards</glossary> whose <command tag="mark">mark</command> <glossary tag="property">property</glossary> is true.</p><p/><p><b>Comments:</b></p><p>You use the <b>mark</b> <glossary tag="command">command</glossary> to set the <keyword tag="marked">marked</keyword> <glossary tag="property">property</glossary> of a subset of <glossary tag="card">cards</glossary>. Once marked, the <glossary tag="card">cards</glossary> can be treated as a set by the <command tag="sort">sort</command> or <command tag="print">print</command> <glossary tag="command">command</glossary>. The form<code> sort marked cards </code>sorts only the cards whose <b>mark</b> <glossary tag="property">property</glossary> is true. The form<code> print marked cards </code>prints only the cards whose <command tag="mark">mark</command> <glossary tag="property">property</glossary> is true.</p><p/><p>In card references, the <b>marked</b> <glossary tag="keyword">keyword</glossary> indicates a card selected from the subset of marked <glossary tag="card">cards</glossary>. For example, the following <glossary tag="statement">statement</glossary> goes to the first <keyword tag="card">card</keyword> in the <glossary tag="current stack">current stack</glossary> whose <command tag="mark">mark</command> <glossary tag="property">property</glossary> is true:</p><p/><p><code>    go first marked card</code></p>
  </description>
</doc>