<doc>
	<legacy_id>1555</legacy_id>
	<name>stdin</name>
	<type>keyword</type>

	<syntax>
	</syntax>

	<synonyms>
	</synonyms>

	<summary>Used with the <command tag="read from file">read from file</command> <glossary tag="command">command</glossary> to designate the <glossary tag="standard input">standard input</glossary>.</summary>

	<examples>
<example>read from stdin until EOF</example>
	</examples>

	<history>
		<introduced version="1.0">Added.</introduced>
		<deprecated version=""></deprecated>
		<removed version=""></removed>
		<changed version="4.6.3">Added Support for Server.</changed>
<changed version="2.0">2.0</changed>
	</history>

	<objects>
	</objects>

	<platforms>
		<mac/>
		<windows/>
		<linux/>
	</platforms>

	<classes>
		<desktop/>
		<server/>
		<web/>
	</classes>

	<security>
	</security>


	<classification>
		<category>The System Environment</category>
		<category>Files, Folders, &amp; Resources</category>
	</classification>

	<references>
		<command tag="read from process">read from process Command</command>
		<keyword tag="stdout">stdout Keyword</keyword>
		<keyword tag="stderr">stderr Keyword</keyword>
	</references>

	<description>
	<overview>Use the <b>stdin</b> <glossary tag="keyword">keyword</glossary> to read data from the standard input, usually the keyboard.</overview>

		<parameters>
		</parameters>

	<value></value>
	<comments>On Unix systems, input to a program is normally read from the keyboard, but can be redirected to come from another device, process or file. This redirection is done on the command line when the program is started up.<p></p><p>On a server in CGI mode, stdin is assumed to contain post data, and reading from it prevents the engine's automatic processing of such information. </p><p></p><p>When you use the <b>read from file</b> <glossary tag="command">command</glossary> with the <b>stdin</b> <glossary tag="keyword">keyword</glossary>, LiveCode reads data from the <glossary tag="standard input">standard input</glossary>.</p><p></p><p></p><change><p>Support for using <b>stdin</b> on <glossary tag="OS X">OS X systems</glossary> was added in version 2.0.</change><p>Support for using <b>stdin</b> on Server was added in version 4.6.3.</p></comments>
	</description>
</doc>