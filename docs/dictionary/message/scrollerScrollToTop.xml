<doc>
	<legacy_id></legacy_id>
	<name>scrollerScrollToTop</name>
	<type>message</type>

	<syntax>
		<example>scrollerScrollToTop</example>
	</syntax>

	<synonyms>
	</synonyms>

	<summary>Sent when a scroller is scrolled to the top by touching the status bar.</summary>

	<examples>
<example><p>on scrollerScrollToTop</p><p>   put &quot;Scrolled to top&quot; into field &quot;scroller status&quot;</p><p>end scrollerScrollToTop</p></example>
	</examples>

	<history>
		<introduced version="4.5.3">Added</introduced>
		<deprecated version=""></deprecated>
		<removed version=""></removed>
		
	</history>

	<objects>
		<iosScroller/>
	</objects>

	<platforms>
		<ios/>
	</platforms>

	<classes>
		<mobile/>
	</classes>

	<security>
	</security>


	<classification>
	</classification>

	<references>
		<function tag="mobileControlGet">mobileControlGet Function</function>
		<function tag="mobileControlTarget">mobileControlTarget Function</function>
		<function tag="mobileControls">mobileControls Function</function>
		<command tag="mobileControlCreate">mobileControlCreate Command</command>
		<command tag="mobileControlDelete">mobileControlDelete Command</command>
		<command tag="mobileControlSet">mobileControlSet Command</command>
		<message tag="scrollerBeginDecelerate">scrollerBeginDecelerate Message</message>
		<message tag="scrollerEndDecelerate">scrollerEndDecelerate Message</message>
		<message tag="scrollerBeginDrag">scrollerBeginDrag Message</message>
		<message tag="scrollerEndDrag">scrollerEndDrag Message</message>
		<message tag="scrollerDidScroll">scrollerDidScroll Message</message>
	</references>

	<description>
	<overview>The <b>scrollerScrollToTop</b> message is sent when a UIScrollView scroller control is scrolled to the top when you touch the status bar.</overview>

		<parameters>
		</parameters>

	<value></value>
	<comments>Handle the <b>scrollerScrollToTop</b> message if you want to move LiveCode controls, make changes to the interface or set the appropriate scroll properties of groups and fields to get a native scrolling effect.<p></p><p>The <b>scrollerScrollToTop</b> message is sent to the object containing the script that created the UIScrollView scroller control.</p></comments>
	</description>
</doc>