<doc>
	<legacy_id>1309</legacy_id>
	<name>selectionChanged</name>
	<type>message</type>

	<syntax>
		<example>selectionChanged</example>
	</syntax>

	<synonyms>
	</synonyms>

	<summary>Sent to a <keyword tag="field">field</keyword> or <keyword tag="player">player</keyword> when the <keyword tag="selection">selection</keyword> is changed.  </summary>

	<examples>
<example><p>on selectionChanged <i>-- display selected char number</i></p><p>  put word 2 of the selectedChunk into field Character Number</p><p>end selectionChanged</p></example>
	</examples>

	<history>
		<introduced version="1.0">Added.  </introduced>
		<deprecated version=""></deprecated>
		<removed version=""></removed>
		
	</history>

	<objects>
		<field/>
	</objects>

	<platforms>
		<mac/>
		<windows/>
		<linux/>
	  <ios/>
    <android/>
  </platforms>

	<classes>
		<desktop/>
		<server/>
		<web/>
	  <mobile/>
  </classes>

	<security>
	</security>


	<classification>
		<category>User Interaction</category>
	</classification>

	<references>
		<command tag="select">select Command</command>
		<property tag="endTime">endTime Property</property>
		<property tag="startTime">startTime Property</property>
		<keyword tag="selection">selection Keyword</keyword>
		<message tag="currentTimeChanged">currentTimeChanged Message</message>
		<message tag="selectedObjectChanged">selectedObjectChanged Message</message>
		<message tag="arrowKey">arrowKey Message</message>
	</references>

	<description>
	<overview>Handle the <b>selectionChanged</b> <keyword tag="message box">message</keyword> if you want to perform updates or do some action when the user changes the <property tag="selected">selected</property> text of a <keyword tag="field">field</keyword> or the <property tag="selected">selected</property> portion of a <keyword tag="player">player</keyword>. </overview>

	<parameters></parameters>

	<value></value>
	<comments>The <b>selectionChanged</b> <keyword tag="message box">message</keyword> is sent to a <keyword tag="field">field</keyword> whenever the user makes a text <keyword tag="selection">selection</keyword> or moves the <glossary tag="insertion point">insertion point</glossary> in the <keyword tag="field">field</keyword>, after the <keyword tag="selection">selection</keyword> is changed. <p></p><p>The <b>selectionChanged</b> <keyword tag="message box">message</keyword> is sent to a <keyword tag="player">player</keyword> whenever the user <glossary tag="select">selects</glossary> a portion of the sound or movie, or when a <glossary tag="handler">handler</glossary> sets the <glossary tag="player">player's</glossary> <property tag="startTime">startTime</property> or <property tag="endTime">endTime</property> <glossary tag="property">property</glossary>. </p><p></p><p></p><note>If an already-selected-item in a list is reselected a <message tag="selectionChanged">selectionChanged message</message> will still be sent. </note></comments>
	</description>
</doc>