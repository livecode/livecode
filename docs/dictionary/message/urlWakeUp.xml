<doc>
	<legacy_id></legacy_id>
	<name>urlWakeUp</name>
	<type>message</type>

	<syntax>
		<example>urlWakeUp <i>wakeUpString</i></example>
	</syntax>

	<synonyms>
	</synonyms>

	<summary>Sent to an application when a custom URL is selected.</summary>

	<examples>
<example><p>on urlWakeUp pURL</p><p>   answer pURL</p><p>end urlWakeUp</p></example>
	</examples>

	<history>
		<introduced version="5.5">Added.</introduced>
		<deprecated version=""></deprecated>
		<removed version=""></removed>
		
	</history>

	<objects>
	</objects>

	<platforms>
		<ios/>
		<android/>
	</platforms>

	<classes>
		<mobile/>
	</classes>

	<security>
	</security>


	<classification>
	</classification>

	<references>
		<function tag="mobileGetLaunchURL">mobileGetLaunchURL Function</function>
	</references>

	<description>
	<overview>Handle the urlWakeUp messags if you want to perform an action when an application is started up using the custom URL scheme.</overview>

		<parameters>
  <parameter>
    <name>urlString</name>
    <description>The custom url that was used to launch the application</description>
  </parameter>		</parameters>

	<value></value>
	<comments>Custom URLs allow an application to be woken up when a specific URL is selected on a device. For example, the url can be a home url of the application web page. <p></p><p>The <b>urlWakeUp</b> message is received by an application when a custom URL is used to launch the application. If the application is not running it is started and then receives the <b>urlWakeUp</b> message, if the application is already running it receives the <b>urlWakeUp</b> message so it can detect that a URL was used to attempt to launch it.</p></comments>
	</description>
</doc>