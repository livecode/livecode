<doc>
	<legacy_id></legacy_id>
	<name>iosPlayer</name>
	<type>object</type>

	<syntax>
	</syntax>

	<synonyms>
	</synonyms>

	<summary>A native iOS control, MPMoviePlayerController, that displays a movie.</summary>

	<examples>
<example><p>mobileControlCreate &quot;player&quot;</p><p></p></example>
<example><p>mobileControlCreate &quot;player&quot;, &quot;movie&quot;</p><p>put the result into sPlayerID</p><p>mobileControlSet sPlayerID, &quot;filename&quot;, tMoviePath</p><p>mobileControlSet sPlayerID, &quot;fullscreen&quot;, &quot;true&quot;</p></example>
	</examples>

	<history>
		<introduced version="4.6">Added.</introduced>
		<deprecated version=""></deprecated>
		<removed version=""></removed>
		
	</history>

	<objects>
		<iosPlayer/>
	</objects>

	<platforms>
		<ios/>
	</platforms>

	<classes>
		<mobile/>
	</classes>

	<security>
	</security>


	<classification>
		<category>Objects &amp; Messages</category>
	</classification>

	<references>
		<function tag="mobileControlGet">mobileControlGet Function</function>
		<function tag="mobileControlTarget">mobileControlTarget Function</function>
		<command tag="mobileControlCreate">mobileControlCreate Command</command>
		<command tag="mobileControlDelete">mobileControlDelete Command</command>
		<command tag="mobileControlSet">mobileControlSet Command</command>
		<command tag="mobileControlDo">mobileControlDo Command</command>
	</references>

	<description>
	<overview>An iosPlayer(MPMoviePlayerController) control is created using a control type of &quot;player&quot;. </overview>

		<parameters>
		</parameters>

	<value></value>
	<comments>On iOS versions prior to 4.2 you can only have a single iosPlayer in existence. Therefore on these iOS versions you can only create a single native player control at any one time, and while one is present you cannot use the <command tag="play video">play video</command> command to play full screen videos.<p></p><p>On iOS versions 4.2 and later, while you can have multiple iosPlayer instances (and this multiple native player controls) simultaneously, only a single one can be playing at any one time. </p><p></p><p></p><note>The iosPlayer control is only available on iOS 4.0 and later.</note><p></p><p></p><note>For full details of what the MPMoviePlayerController is capable of, and background about it see the iOS reference document.</note></comments>
	</description>
</doc>