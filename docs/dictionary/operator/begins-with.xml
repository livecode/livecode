<doc>
	<legacy_id>3383</legacy_id>
	<name>begins with</name>
	<type>operator</type>

	<syntax>
		<example><i>string</i> begins with <i>prefix</i></example>
	</syntax>

	<synonyms>
	</synonyms>

	<summary>The <b>begins with</b> operator takes two strings and returns true if the second string matches the start of the first string. </summary>

	<examples>
<example>&quot;foobar&quot; begins with &quot;foo&quot; -- <i>evaluates to true</i></example>
<example>&quot;foobar&quot; begins with &quot;food&quot; -- <i>evaluates to false</i></example>
	</examples>

	<history>
		<introduced version="2.9">Added. </introduced>
		<deprecated version=""></deprecated>
		<removed version=""></removed>
		
	</history>

	<objects>
	</objects>

	<platforms>
		<mac/>
		<windows/>
		<linux/>
	  <ios/>
    <android/>
  </platforms>

	<classes>
		<desktop/>
		<server/>
		<web/>
	  <mobile/>
  </classes>

	<security>
	</security>


	<classification>
	</classification>

	<references>
		<function tag="length">length Function</function>
		<function tag="offset">offset Function</function>
		<operator tag="contains">contains Operator</operator>
		<operator tag="ends with">ends with Property</operator>
	</references>

	<description>
	<overview>For the expression to evaluate to true the whole of <i>prefix</i> must be present at the beginning of <i>string</i>. For example, if <i>prefix</i> has three characters these must match the first three characters of <i>string</i> (in order).<p></p><p>This operator takes into consideration the current value of the <property tag="caseSensitive">caseSensitive property</property>.</p></overview>

		<parameters>
		</parameters>

	<value></value>
	<comments></comments>
	</description>
</doc>