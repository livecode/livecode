<doc>
	<legacy_id>3382</legacy_id>
	<name>ends with</name>
	<type>operator</type>

	<syntax>
		<example><i>string </i>ends with<i> suffix</i></example>
	</syntax>

	<synonyms>
	</synonyms>

	<summary>The ends with operator takes two strings and returns true if the second string matches the end of the first string. </summary>

	<examples>
<example>&quot;foobar&quot; ends with &quot;bar&quot; -- evaluates to true</example>
<example>&quot;foobar&quot; ends with &quot;baz&quot; -- evaluates to false</example>
	</examples>

	<history>
		<introduced version="2.9">Added. </introduced>
		<deprecated version=""></deprecated>
		<removed version=""></removed>
		
	</history>

	<objects>
	</objects>

	<platforms>
		<mac/>
		<windows/>
		<linux/>
	  <ios/>
    <android/>
  </platforms>

	<classes>
		<desktop/>
		<server/>
		<web/>
	  <mobile/>
  </classes>

	<security>
	</security>


	<classification>
	</classification>

	<references>
		<function tag="length">length Function</function>
		<function tag="offset">offset Function</function>
		<operator tag="contains">contains Operator</operator>
		<operator tag="begins with">begins with Operator</operator>
	</references>

	<description>
	<overview>For the expression to evaluate to true <i>suffix</i> must match the end of <i>string</i>. For example, if <i>suffix</i> contains three characters these must match the the last three characters of <i>string </i>(in order).  <p></p><p>This takes into consideration the value of the <property tag="caseSensitive">caseSensitive property</property>. </p></overview>

	<parameters></parameters>

	<value></value>
	<comments></comments>
	</description>
</doc>