<doc>
	<legacy_id>1290</legacy_id>
	<name>is among</name>
	<type>operator</type>

	<syntax>
		<example>valueToFind is among the {paragraphs | sentences | lines | items | words | segments | trueWords | tokens | codePoints | codeUnits | bytes} of stringToSearch</example>
		<example></example>
		<example></example>
	</syntax>

	<synonyms>
	</synonyms>

	<summary>Evaluates to true if a chunk is equal to one of the chunks in a string, false otherwise.</summary>

	<examples>
<example><p>"Hello" is among the words of "Hello World" -- evaluates to true</p></example>
<example><p>"Hell" is among the words of "Hello World" -- evaluates to false</p></example>
<example><p></p></example>
<example><p></p></example>
<example><p></p></example>
	</examples>

	<history>
		<introduced version="1.0">Added.</introduced>
		<deprecated version=""></deprecated>
		<removed version=""></removed>
		
		<experimental version=""></experimental>
		<nonexperimental version=""></nonexperimental>
	</history>

	<objects>
	</objects>

	<platforms>
		<mac/>
		<windows/>
		<linux/>
		<ios/>
		<android/>
	</platforms>

	<classes>
		<desktop/>
		<server/>
		<web/>
		<mobile/>
	</classes>

	<security>
	</security>


	<classification>
		<category>Text and Data Processing</category>
		<category></category>
		<category></category>
	</classification>

	<references>
		<function tag="wordOffset">wordOffset function</function>
		<property tag="wholeMatches">wholeMatches property</property>
		<operator tag="contains">contains operator</operator>
		<operator tag="is not among">is not among operator</operator>
		<operator tag="is in">is in operator</operator>
		<operator tag="is among the keys of">is among the keys of operator</operator>
	</references>

	<description><p>Use the <b>is among</b> operator to find out whether a string exists as an entire word, item, or line of another string.</p><p></p><p><b>Parameters:</b></p><p>The <i>valueToFind</i> is a string or an expression that evaluates to a string.</p><p></p><p>The <i>stringToSearch</i> is a string or an expression that evaluates to a string.</p><p></p><p><b>Comments:</b></p><p>The <i>valueToFind</i> must be a single word, item, or line (whichever chunk type you have specified). If you specify a string containing more than one chunk, the <b>is among</b> operator evaluates to false, even if all the chunks are in the <i>stringToSearch</i>.</p><p></p><p>The <b>is among</b> operator is the logical inverse of the is not among operator. When one is true, the other is false.</p><p></p><p>'is among the bytes' should be used to check if a binary strings contains the given bytes.</p></description>
</doc>