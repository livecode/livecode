<doc>
  <legacy_id>1984</legacy_id>
  <name>is in</name>
  <type>operator</type>
  <syntax>
    <example><i>subString</i> is in <i>string</i></example>
  </syntax>
  <library></library>
  <objects>
  </objects>
  <synonyms>
  </synonyms>
  <classification>
    <category>Text and Data Processing</category>
  </classification>
  <references>
    <operator tag="contains">contains Operator</operator>
    <operator tag="is among">is among Operator</operator>
    <operator tag="is not in">is not in Operator</operator>
  </references>
  <history>
    <introduced version="1.0">Added.</introduced>
  </history>
  <platforms>
    <mac/>
    <windows/>
    <linux/>
    <ios/>
    <android/>
  </platforms>
  <classes>
    <desktop/>
    <server/>
    <web/>
    <mobile/>
  </classes>
  <security>
  </security>
  <summary>Compares two <glossary tag="string">strings</glossary> and <glossary tag="evaluate">evaluates</glossary> to true if the second contains the first, false if not.</summary>
  <examples>
    <example>"A" is in "ABC" <code><i>-- evaluates to true</i></code></example>
    <example>"123" is in "13" <code><i>-- evaluates to false</i></code></example>
  </examples>
  <description>
    <p>Use the <b>is in</b> <glossary tag="operator">operator</glossary> to find out whether a <keyword tag="string">string</keyword> contains a substring.</p><p/><p><b>Parameters:</b></p><p>The <i>substring</i> and <i>string</i> are both <glossary tag="string">strings</glossary>, or <glossary tag="expression">expressions</glossary> that <glossary tag="evaluate">evaluate</glossary> to <glossary tag="string">strings</glossary>.</p><p/><p><b>Comments:</b></p><p>The expression</p><p><code><i>firstString</i> is in<i> secondString</i></code></p><p>is equivalent t<code>o</code></p><p><code><i>  secondString</i> contain<i>s firstString</i></code></p><p/><p>The <b>is in</b> <glossary tag="operator">operator</glossary> is the logical inverse of the <operator tag="is not in">is not in</operator> <glossary tag="operator">operator</glossary>. When one is true, the other is false.</p>
  </description>
</doc>