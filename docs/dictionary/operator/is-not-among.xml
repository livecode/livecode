<doc>
	<legacy_id>1540</legacy_id>
	<name>is not among</name>
	<type>operator</type>

	<syntax>
		<example>valueToFind is not among the {paragraphs | sentences | lines | items | words | segments | trueWords | tokens | codePoints | codeUnits | bytes} of stringToSearch</example>
		<example></example>
	</syntax>

	<synonyms>
	</synonyms>

	<summary>Evaluates to true if a chunk is not equal to any of the chunks in a string, false otherwise.</summary>

	<examples>
<example><p>"a" is not among the words of "human" -- evaluates to true</p></example>
<example><p></p></example>
<example><p></p></example>
<example><p></p></example>
<example><p></p></example>
	</examples>

	<history>
		<introduced version="1.0">Added.</introduced>
		<deprecated version=""></deprecated>
		<removed version=""></removed>
		
		<experimental version=""></experimental>
		<nonexperimental version=""></nonexperimental>
	</history>

	<objects>
	</objects>

	<platforms>
		<mac/>
		<windows/>
		<linux/>
		<ios/>
		<android/>
	</platforms>

	<classes>
		<desktop/>
		<server/>
		<web/>
		<mobile/>
	</classes>

	<security>
	</security>


	<classification>
		<category>Text and Data Processing</category>
		<category></category>
	</classification>

	<references>
		<operator tag="contains">contains operator</operator>
		<operator tag="is among">is among operator</operator>
		<operator tag="is among the keys of">is among the keys of operator</operator>
		<keyword tag="characters">characters keyword</keyword>
	</references>

	<description><p>Use the <b>is not among</b> operator to find out whether a string exists as an entire word, item, or line of another string.</p><p></p><p><b>Parameters:</b></p><p>The <i>valueToFind</i> is a string or an expression that evaluates to a string.</p><p></p><p>The <i>stringToSearch</i> is a string is an expression that evaluates to a string.</p><p></p><p><b>Comments:</b></p><p>The <i>valueToFind</i> must be a single word, item, or line (whichever chunk type you have specified). If you specify a string containing more than one chunk, the <b>is not among</b> operator evaluates to true, even if all the chunks are in the <i>stringToSearch</i>.</p><p></p><p>The <b>is not among</b> operator is the logical inverse of the is among operator. When one is true, the other is false.</p></description>
</doc>