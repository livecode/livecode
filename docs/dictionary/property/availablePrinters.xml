<doc>
  <legacy_id>3081</legacy_id>
  <name>availablePrinters</name>
  <type>property</type>
  <syntax>
    <example>get the availablePrinters</example>
  </syntax>
  <library></library>
  <objects>
    <global/>
  </objects>
  <synonyms>
  </synonyms>
  <classification>
    <category>Printing</category>
  </classification>
  <references>
    <command tag="answer printer">answer printer Command</command>
  </references>
  <history>
    <introduced version="2.9">Added.</introduced>
  </history>
  <platforms>
    <mac/>
    <windows/>
    <linux/>
  </platforms>
  <classes>
    <desktop/>
    <web/>
  </classes>
  <security>
    <printing/>
  </security>
  <summary>Reports a list of all the printers available on the current system.</summary>
  <examples>
    <example>if tPrinter is not among the lines of the availablePrinters then</p><p>  answer error "Printer not found"</p><p>end if</example>
    <example>set the printerName to (line 1 of the availablePrinters)</example>
  </examples>
  <description>
    <p>Use the <b>availablePrinters</b> property to find out the names of the printers installed on the current system.</p><p/><p><b>Value:</b></p><p>The <b>availablePrinters</b> is a return-delimited list of printer names (i.e. one per line).</p><p/><p><b>Comments:</b></p><p>The <b>availablePrinters</b> property is read-only and cannot be set.</p>
  </description>
</doc>