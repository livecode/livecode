<doc>
  <legacy_id>1947</legacy_id>
  <name>boundingRect</name>
  <type>property</type>
  <syntax>
    <example>set the boundingRect of <i>group</i> to {<i>left</i>,<i>top</i>,<i>right</i>,<i>bottom</i> | empty }</example>
  </syntax>
  <library></library>
  <objects>
    <group/>
  </objects>
  <synonyms>
  </synonyms>
  <classification>
    <category>Appearance &amp; Positioning</category>
  </classification>
  <references>
    <property tag="margins">margins Property</property>
  </references>
  <history>
    <introduced version="2.0">Added.</introduced>
  </history>
  <platforms>
    <mac/>
    <windows/>
    <linux/>
    <ios/>
    <android/>
  </platforms>
  <classes>
    <desktop/>
    <server/>
    <web/>
    <mobile/>
  </classes>
  <security>
  </security>
  <summary>Specifies whether a <command tag="group">group</command> automatically changes size to fit when its <glossary tag="control">controls</glossary> are moved or resized.</summary>
  <examples>
    <example>set the boundingRect of group "Icons" to 100,100,500,500</example>
    <example>set the boundingRect of the target to the rect of the target</example>
  </examples>
  <description>
    <p>Use the <b>boundingRect</b> <glossary tag="property">property</glossary> to <keyword tag="control">control</keyword> how a <command tag="group">group</command> responds when you move one of the <glossary tag="group">group's</glossary> <glossary tag="control">controls</glossary> to the edge of the <command tag="group">group</command>.</p><p/><p><b>Value:</b></p><p>The <b>boundingRect</b> of a <command tag="group">group</command> consists of four <glossary tag="integer">integers</glossary> separated by commas.</p><p/><p>By default, the <b>boundingRect</b> <glossary tag="property">property</glossary> of a <command tag="group">group</command> is set to empty.</p><p/><p><b>Comments:</b></p><p>If a group's <b>boundingRect</b> is empty and its <property tag="lockLocation">lockLocation</property> is false, when you drag an <glossary tag="object">object</glossary> toward the boundary of the <command tag="group">group</command>, the <command tag="group">group</command> automatically expands, resizing itself to fit. If the <property tag="lockLocation">lockLocation</property> is true, the <glossary tag="object">object</glossary> is clipped to the <glossary tag="group">group's</glossary> <keyword tag="rectangle">rectangle</keyword>.</p><p/><p>If a group's <b>boundingRect</b> is not empty and its <property tag="lockLocation">lockLocation</property> is false, when you drag an <glossary tag="object">object</glossary> toward the boundary of the <command tag="group">group</command>, the <command tag="group">group</command> does not automatically resize to fit its <glossary tag="object">objects</glossary>. Instead, the <glossary tag="object">object</glossary> is clipped at the <property tag="boundingRect">boundingRect</property>. (In <href tag="../glossary/groupdashediting_mode.xml">group-editing mode</href>, the entire <keyword tag="control">control</keyword> is shown, but when you exit <href tag="../glossary/groupdashediting_mode.xml">group-editing mode</href>, <glossary tag="control">controls</glossary> outside the <property tag="boundingRect">boundingRect</property> are clipped.)</p><p/><p>If the group is a scrolling group, dragging an object in it automatically scrolls the group. When you drag beyond the scrollable area, the object is clipped.</p>
  </description>
</doc>