<doc>
  <legacy_id>1565</legacy_id>
  <name>cantSelect</name>
  <type>property</type>
  <syntax>
    <example>set the cantSelect of <i>object</i> to {true | false}</example>
  </syntax>
  <library></library>
  <objects>
  </objects>
  <synonyms>
  </synonyms>
  <classification>
    <category>Developing with LiveCode</category>
  </classification>
  <references>
    <command tag="disable">disable Command</command>
    <command tag="modeless">modeless Command</command>
  </references>
  <history>
    <introduced version="1.0">Added.</introduced>
  </history>
  <platforms>
    <mac/>
    <windows/>
    <linux/>
    <ios/>
    <android/>
  </platforms>
  <classes>
    <desktop/>
    <server/>
    <web/>
    <mobile/>
  </classes>
  <security>
  </security>
  <summary>Specifies whether a <keyword tag="control">control</keyword> can be <property tag="selected">selected</property> with the Pointer <function tag="tool">tool</function>.</summary>
  <examples>
    <example>set the cantSelect of field "Background" to true</example>
  </examples>
  <description>
    <p>Use the <b>cantSelect</b> <glossary tag="property">property</glossary> to protect a <keyword tag="control">control</keyword> from being changed by the user.</p><p/><p><b>Value:</b></p><p>The <b>cantSelect</b> of a <keyword tag="control">control</keyword> is true or false.</p><p/><p>By default, the <b>cantSelect</b> of a newly created <keyword tag="control">control</keyword> is false.</p><p/><p><b>Comments:</b></p><p>If the <b>cantSelect</b> of a <keyword tag="control">control</keyword> is set to true, the user cannot <command tag="select">select</command> it (and so cannot resize it or move it).  If a group or card has <b>cantSelect</b> set to true, all of its children will also act as if they have <b>cantSelect</b> set to true (i.e. They will also not be selectable.)  You can use 'the <keyword tag="effective">effective</keyword> <b>cantSelect</b>' to determine if the object, or any of its ancestors have <b>cantSelect</b> set.</p><p/><p>When the user clicks the control with the <glossary tag="Pointer tool">Pointer tool</glossary>, LiveCode acts as though it has been clicked with the <glossary tag="Browse tool">Browse tool</glossary>. If the <b>cantSelect</b> is false, the user can click the <keyword tag="control">control</keyword> with the <glossary tag="Pointer tool">Pointer tool</glossary> to <command tag="select">select</command> it.</p><p/><p>A handler can still select the object, regardless of the setting of the <b>cantSelect</b> <glossary tag="property">property</glossary>.</p>
  </description>
</doc>