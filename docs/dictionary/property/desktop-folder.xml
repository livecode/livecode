<doc>
	<legacy_id></legacy_id>
	<name>desktop folder</name>
	<type>property</type>

	<syntax>
		<example>the desktop folder</example>
	</syntax>

	<synonyms>
	</synonyms>

	<summary>Returns the path to the current user's desktop folder.</summary>

	<examples>
<example>get the desktop folder</example>
<example>put the desktop folder &amp; &quot;/log.txt&quot; into tLogFile</example>
	</examples>

	<history>
		<introduced version="5.0.2">Added.</introduced>
		<deprecated version=""></deprecated>
		<removed version=""></removed>
		
	</history>

	<objects>
	</objects>

	<platforms>
		<mac/>
		<windows/>
		<linux/>
	</platforms>

	<classes>
		<desktop/>
	</classes>

	<security>
	</security>


	<classification>
	</classification>

	<references>
		<function tag="specialFolderPath">specialFolderPath Function</function>
		<property tag="engine folder">the engine folder Property</property>
		<property tag="home folder">the home folder Property</property>
		<property tag="documents folder">the documents folder Property</property>
		<property tag="temporary folder">the temporary folder Property</property>
	</references>

	<description>
	<overview>Returns the full path to the current user's desktop folder.</overview>

		<parameters>
		</parameters>

	<value></value>
	<comments><b>The documents folder</b> property returns the path to the user's desktop folder.<p></p><p>This property is read only and is equivalent to the specialFolderPath(&quot;desktop&quot;) function.</p></comments>
	</description>
</doc>