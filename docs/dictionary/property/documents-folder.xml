<doc>
	<legacy_id></legacy_id>
	<name>documents folder</name>
	<type>property</type>

	<syntax>
		<example>the documents folder</example>
	</syntax>

	<synonyms>
	</synonyms>

	<summary>Returns the path to the current user's documents folder.</summary>

	<examples>
<example>get the documents folder</example>
<example>put the documents folder &amp; &quot;/music&quot; into tUserMusicFolder</example>
	</examples>

	<history>
		<introduced version="5.0.2">Added.</introduced>
		<deprecated version=""></deprecated>
		<removed version=""></removed>
		
	</history>

	<objects>
	</objects>

	<platforms>
		<mac/>
		<windows/>
		<ios/>
		<android/>
	</platforms>

	<classes>
		<desktop/>
		<mobile/>
	</classes>

	<security>
	</security>


	<classification>
	</classification>

	<references>
		<function tag="specialFolderPath">specialFolderPath Function</function>
		<property tag="engine folder">the engine folder Property</property>
		<property tag="home folder">the home folder Property</property>
		<property tag="desktop folder">the desktop folder Property</property>
		<property tag="temporary folder">the temporary folder Property</property>
	</references>

	<description>
	<overview>Returns the full path to the current user's documents folder.</overview>

		<parameters>
		</parameters>

	<value></value>
	<comments>On Mac OS X and Windows <b>the documents folder</b> property returns the path to the user's documents folder.<p></p><p>On iOS <b>the documents folder </b>property returns the folder in which the application should store any document data (this folder is backed up by iTunes on sync). </p><p></p><p>On Android <b>the documents folder</b> property returns the path to a folder that can be used for per-application data files.</p><p></p><p>This property is read only and is equivalent to the specialFolderPath(&quot;documents&quot;) function.</p></comments>
	</description>
</doc>