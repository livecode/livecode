<doc>
	<legacy_id></legacy_id>
	<name>encoding</name>
	<type>property</type>

	<syntax>
		<example>the encoding of { <i>button | graphic | group </i>}</example>
		<example>the encoding of [<i>chunk</i> of] <i>field</i></example>
	</syntax>

	<synonyms>
	</synonyms>

	<summary>Returns the encoding of the specified piece of text.</summary>

	<examples>
<example>get the encoding of button &quot;myButton&quot;</example>
<example>put the encoding of word 1 of field &quot;language&quot; into tEncoding</example>
	</examples>

	<history>
		<introduced version="5.5">Added.</introduced>
		<deprecated version=""></deprecated>
		<removed version=""></removed>
		
	</history>

	<objects>
		<group/>
		<field/>
		<button/>
		<graphic/>
	</objects>

	<platforms>
		<mac/>
		<windows/>
		<linux/>
		<ios/>
		<android/>
	</platforms>

	<classes>
		<mobile/>
	</classes>

	<security>
	</security>


	<classification>
	</classification>

	<references>
		<property tag="text">text Property</property>
		<property tag="unicodeText">unicodeText Property</property>
		<property tag="label">label Property</property>
		<property tag="unicodeLabel">unicodeLabel Property</property>
	</references>

	<description>
	<overview>Use the <b>encoding</b> property to retrieve the encoding of the specified object or field chunk.</overview>

		<parameters>
		</parameters>

	<value>Returns native<b>, </b>unicode<b> </b>or<b> </b>mixed<b> </b>depending on the encoding of the specified object or field chunk.</value>
	<comments>The <b>encoding</b> property of a button, graphic or group returns native if the engine is storing the label or text in the native encoding and unicode otherwise.<p></p><p>The <b>encoding</b> property of a field, or chunk of a field, returns native if the specified chunk is stored in native encoding, unicode if it is UTF-16, and mixed if it is a combination of the two.</p><p></p><p></p><note>The <b>encoding</b> property is read only and cannot be set.</note></comments>
	</description>
</doc>