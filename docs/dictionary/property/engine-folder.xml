<doc>
	<legacy_id></legacy_id>
	<name>engine folder</name>
	<type>property</type>

	<syntax>
		<example>the engine folder</example>
	</syntax>

	<synonyms>
	</synonyms>

	<summary>Returns the path to the engine folder on mobile devices.</summary>

	<examples>
<example>get the engine folder</example>
<example>put the engine folder &amp; &quot;/assets/sound1.mp3&quot; into tSoundFile</example>
	</examples>

	<history>
		<introduced version="5.0.2">Added.</introduced>
		<deprecated version=""></deprecated>
		<removed version=""></removed>
		
	</history>

	<objects>
	</objects>

	<platforms>
		<ios/>
		<android/>
	</platforms>

	<classes>
		<mobile/>
	</classes>

	<security>
	</security>


	<classification>
	</classification>

	<references>
		<function tag="specialFolderPath">specialFolderPath Function</function>
		<property tag="home folder">the home folder Property</property>
		<property tag="documents folder">the documents  folder Property</property>
		<property tag="desktop folder">the desktop  folder Property</property>
		<property tag="temporary folder">the temporary  folder Property</property>
	</references>

	<description>
	<overview>Returns the full path to the engine folder.</overview>

		<parameters>
		</parameters>

	<value></value>
	<comments>On iOS <b>the engine folder </b>property returns the path to the folder containing the built standalone engine (i.e. the bundle). This is useful for constructing paths to resources that have been copied into the bundle at build time.<p></p><p>On Android <b>the engine folder</b> property returns the (virtual) path to the engine and its assets.</p><p></p><p>This property is read only and is equivalent to the specialFolderPath(&quot;engine&quot;) function.</p></comments>
	</description>
</doc>