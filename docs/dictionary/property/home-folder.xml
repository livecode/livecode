<doc>
	<legacy_id></legacy_id>
	<name>home folder</name>
	<type>property</type>

	<syntax>
		<example>the home folder</example>
	</syntax>

	<synonyms>
	</synonyms>

	<summary>Returns the path to the current user's home directory(on desktop) or the folder containing the application bundle and its associated data and folders(on iOS)</summary>

	<examples>
<example>get the home folder</example>
<example>put the home folder &amp; &quot;/Projects&quot; into tUserProjectsFolder</example>
	</examples>

	<history>
		<introduced version="5.0.2">Added.</introduced>
		<deprecated version=""></deprecated>
		<removed version=""></removed>
		
	</history>

	<objects>
	</objects>

	<platforms>
		<mac/>
		<windows/>
		<linux/>
		<ios/>
	</platforms>

	<classes>
		<desktop/>
		<mobile/>
	</classes>

	<security>
	</security>


	<classification>
	</classification>

	<references>
		<function tag="specialFolderPath">specialFolderPath Function</function>
		<property tag="engine folder">the engine folder Property</property>
		<property tag="documents folder">the documents  folder Property</property>
		<property tag="desktop folder">the desktop  folder Property</property>
		<property tag="temporary folder">the temporary  folder Property</property>
	</references>

	<description>
	<overview>Returns the path to the current user's home directory(on desktop) or the folder containing the application bundle and its associated data and folders(on iOS).</overview>

		<parameters>
		</parameters>

	<value></value>
	<comments>On Mac OS X, Windows and Linux systems <b>the home folder</b> property returns the path to the current user's home directory, e.g. &quot;/Users/Fred&quot; or &quot;C:/Documents and Settings/Fred&quot;.<p></p><p>On iOS <b>the home folder </b>property returns the path to the (unique) folder containing the application bundle and its associated data and folders.</p><p></p><p>This property is read only and is equivalent to the specialFolderPath(&quot;home&quot;) function.</p></comments>
	</description>
</doc>