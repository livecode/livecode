<doc>
  <legacy_id>1455</legacy_id>
  <name>mediaTypes</name>
  <type>property</type>
  <syntax>
    <example>get the mediaTypes of <i>player</i></example>
  </syntax>
  <library></library>
  <objects>
    <player/>
  </objects>
  <synonyms>
  </synonyms>
  <classification>
    <category>Images &amp; Multimedia</category>
  </classification>
  <references>
    <property tag="trackCount">trackCount Property</property>
  </references>
  <history>
    <introduced version="1.0">Added.</introduced>
  </history>
  <platforms>
    <mac/>
    <windows/>
    <linux/>
    <ios/>
    <android/>
  </platforms>
  <classes>
    <desktop/>
    <server/>
    <web/>
    <mobile/>
  </classes>
  <security>
  </security>
  <summary>Specifies one or more types of media used in in a <glossary tag="QuickTime">QuickTime</glossary> movie.</summary>
  <examples>
    <example>if the mediaTypes of player 1 contains "audio" then increaseLoudness</example>
  </examples>
  <description>
    <p>Use the <b>mediaTypes</b> <glossary tag="property">property</glossary> to find out what kinds of media a <glossary tag="QuickTime">QuickTime</glossary> movie contains.</p><p/><p><b>Value:</b></p><p>The <b>mediaTypes</b> is a list of one or more of the following, separated by commas:</p><p/><p>        * <code>video</code></p><p>        * <code>audio</code></p><p>        * <code>text</code></p><p>        * <code>qtvr</code></p><p>        * <code>sprite</code></p><p>        * <code>flash</code></p><p/><p>This property is read-only and cannot be set.</p><p/><p><b>Comments:</b></p><p>Most QuickTime movies provide more than one media type. For example, a typical movie might provide one video and one audio track.</p><p/><p>If more than one track in the movie uses the same media type, the media type is specified only once in the <b>mediaTypes</b> property. For example, "<code>audio</code>" appears only once in the <b>mediaTypes</b>, even if the movie contains more than one audio track.</p>
  </description>
</doc>