<doc>
	<legacy_id></legacy_id>
	<name>plainText</name>
	<type>property</type>

	<syntax>
		<example>set the plainText of [<i>chunk</i> of] <i>field </i>to <i>string</i></example>
		<example>get the plainText of [<i>chunk</i> of] <i>field</i></example>
	</syntax>

	<synonyms>
	</synonyms>

	<summary>Returns the contents of a field chunk as plain text.</summary>

	<examples>
<example>write the plainText of field 1 to file myTextFile</example>
<example><p>get the plainText of field &quot;Field&quot;</p><p>answer it with &quot;okay&quot;</p></example>
<example><p>put the plainText of word 2 to -2 of field &quot;list&quot;</p></example>
	</examples>

	<history>
		<introduced version="4.6">Added.</introduced>
		<deprecated version=""></deprecated>
		<removed version=""></removed>
		<changed version="5.5">Now works at field chunk level</changed>
	</history>

	<objects>
		<field/>
	</objects>

	<platforms>
		<mac/>
		<windows/>
		<linux/>
	</platforms>

	<classes>
		<desktop/>
		<server/>
		<web/>
	</classes>

	<security>
	</security>


	<classification>
		<category>Text and Data Processing</category>
	</classification>

	<references>
		<property tag="dontWrap">dontWrap Property</property>
		<property tag="formattedText">formattedText Property</property>
		<property tag="unicodeFormattedText">unicodeFormattedText Property</property>
		<property tag="unicodePlainText">unicodePlainText Property</property>
	</references>

	<description>
	<overview>Use the <b>plainText</b> <glossary tag="property">property</glossary> to get the content of a field as plain text with any listStyle properties being converted appropriately into plain-text for the paragraphs the affect.</overview>

		<parameters>
		</parameters>

	<value>The <b>plainText</b> of a <keyword tag="field">field</keyword> is a string.</value>
	<comments>When you get a <keyword tag="field">field's</keyword> <b>plainText</b>, the <keyword tag="field">field's</keyword> text is converted to plain text with any listStyle property being converted appropriately into plain-text.<p></p><p>Any paragraphs with listStyle set are prefixed by an appropriate plain-text form of the bullet or index. The property returns text encoded in the native platform encoding with '?' replacing any non-convertible characters.</p></comments>
	</description>
</doc>