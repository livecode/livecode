<doc>
  <legacy_id>3085</legacy_id>
  <name>printPaperRectangle</name>
  <type>property</type>
  <syntax>
    <example>get the printPaperRectangle</example>
  </syntax>
  <library></library>
  <objects>
    <global/>
  </objects>
  <synonyms>
    <synonym>printPaperRect</synonym>
  </synonyms>
  <classification>
    <category>Printing</category>
  </classification>
  <references>
    <command tag="answer printer">answer printer Command</command>
    <property tag="printRectangle">printRectangle Property</property>
  </references>
  <history>
    <introduced version="2.9">Added.</introduced>
  </history>
  <platforms>
    <mac/>
    <windows/>
    <linux/>
    <ios/>
  </platforms>
  <classes>
    <desktop/>
    <server/>
    <web/>
    <mobile/>
  </classes>
  <security>
    <printing/>
  </security>
  <summary>Reports the size of the page that will be printed.</summary>
  <examples>
    <example>put the printPaperRectangle into tRectangle</example>
  </examples>
  <description>
    <p>Use the <b>printPaperRectangle</b> property to find out the rectangle being printed to in device-co-ordinates, taking the printPaperOrientation into account.</p><p/><p><b>Value:</b></p><p>The <b>printPaperRectangle</b> property contains four comma-separated integers and is in points. The default value reflects the default values of the printPaperSize and printPaperOrientation.  </p><p/><p>The four numbers represent the <i>left</i>, <i>top</i>, <i>right</i> and <i>bottom</i> of the rectangle being printed to. The rectangle is relative to the top-left of the page, and the <i>left</i> and <i>top</i> will currently always be 0.</p><p/><p><b>Comments:</b></p><p>The printPaperRectangle property is read-only and cannot be set. Changing the <property tag="printMargins">printMargins</property> will not affect the <b>printPaperRectangle</b>.</p>
  </description>
</doc>