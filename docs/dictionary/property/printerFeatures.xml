<doc>
  <legacy_id>3083</legacy_id>
  <name>printerFeatures</name>
  <type>property</type>
  <syntax>
    <example>get the printerFeatures</example>
  </syntax>
  <library></library>
  <objects>
    <global/>
  </objects>
  <synonyms>
  </synonyms>
  <classification>
    <category>Printing</category>
  </classification>
  <references>
    <command tag="answer printer">answer printer Command</command>
    <property tag="printDuplex">printDuplex Property</property>
  </references>
  <history>
    <introduced version="2.9">Added.</introduced>
  </history>
  <platforms>
    <mac/>
    <windows/>
    <linux/>
  </platforms>
  <classes>
    <desktop/>
    <server/>
    <web/>
  </classes>
  <security>
    <printing/>
  </security>
  <summary>Reports a list of features that the current printer supports.</summary>
  <examples>
    <example>if "color" is among the items of the printerFeatures then</p><p>  set the enabled of button "Color" of group "Print Settings" of me to true</p><p>end if</example>
  </examples>
  <description>
    <p>Use the <b>printerFeatures</b> property to find out which printing features are supported by the currently selected printer.</p><p/><p><b>Value:</b></p><p>The <b>printerFeatures</b> returns a comma-separated list containing zero or more of the following features.</p><p/><p> - <i>collate</i>: the printCollate property will have an effect (if not present it means the printer cannot collate multiple copies automatically).</p><p/><p> - <i>copies</i>: the printCopies property will have an effect (if not present it means the printer will only print one copy).</p><p/><p> - <i>color</i>: the printColors property will have an effect (if not present, it means the printer will only print in monochrome).</p><p/><p> - <i>duplex</i>: the printDuplex property will have an effect (if not present, it means the printer cannot print double-sided).</p><p/><p><b>Comments:</b></p><p>The <b>printerFeatures</b> property is read-only and cannot be set.</p>
  </description>
</doc>