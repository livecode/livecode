<doc>
  <legacy_id>3082</legacy_id>
  <name>printerName</name>
  <type>property</type>
  <syntax>
    <example>set the printerName to <i>string</i></example>
    <example>get the printerName</example>
  </syntax>
  <library></library>
  <objects>
    <global/>
  </objects>
  <synonyms>
  </synonyms>
  <classification>
    <category>Printing</category>
  </classification>
  <references>
    <command tag="answer printer">answer printer Command</command>
    <property tag="printTitle">printTitle Property</property>
  </references>
  <history>
    <introduced version="2.9">Added.</introduced>
  </history>
  <platforms>
    <mac/>
    <windows/>
    <linux/>
  </platforms>
  <classes>
    <desktop/>
    <server/>
    <web/>
  </classes>
  <security>
    <printing/>
  </security>
  <summary>Reports the name of the currently selected printer.</summary>
  <examples>
    <example>set the printerName to "PostScript Printer"</example>
    <example>if the printerName is not empty then</p><p>  enable button "Print" of me</p><p>end if</example>
  </examples>
  <description>
    <p>Use the <b>printerName</b> property to determine or set the printer that will be used by LiveCode.</p><p/><p><b>Value:</b></p><p>The <b>printerName</b> property returns the name of the currently selected printer, or empty if printing is not available. By default, the name of the system's default printer is returned.</p><p/><p><b>Comments:</b></p><p>Setting the <b>printerName</b> will attempt to select the printer with the given name and reset all printer properties to the defaults for that printer. If the printer cannot be found, the <function tag="result">result</function> is set to "unknown printer"</p><p/><p>Setting the <b>printerName</b> to empty will reset the printer to the system default, and all printer settings to the default for that printer.</p><p/><p>To have an effect, this property must be set before calling <command tag="open printing">open printing</command>.</p>
  </description>
</doc>