<doc>
  <legacy_id>2176</legacy_id>
  <name>randomSeed</name>
  <type>property</type>
  <syntax>
    <example>set the randomSeed to <i>integer</i></example>
  </syntax>
  <library></library>
  <objects>
    <global/>
  </objects>
  <synonyms>
  </synonyms>
  <classification>
    <category>Math, Logic, &amp; Dates</category>
  </classification>
  <references>
    <function tag="random">random Function</function>
  </references>
  <history>
    <introduced version="1.0">Added.</introduced>
  </history>
  <platforms>
    <mac/>
    <windows/>
    <linux/>
    <ios/>
    <android/>
  </platforms>
  <classes>
    <desktop/>
    <server/>
    <web/>
    <mobile/>
  </classes>
  <security>
  </security>
  <summary>Determines the seed <function tag="value">value</function> used to generate <function tag="random">random</function> numbers.</summary>
  <examples>
    <example>set the randomSeed to 4570422</example>
  </examples>
  <description>
    <p>Use the <b>randomSeed</b> <glossary tag="property">property</glossary> to ensure greater randomness when generating <function tag="random">random</function> numbers.</p><p/><p><b>Value:</b></p><p>The <b>randomSeed</b> <glossary tag="property">property</glossary> is an <keyword tag="integer">integer</keyword>.</p><p/><p><b>Comments:</b></p><p>Changing the <b>randomSeed</b> <glossary tag="property">property</glossary> changes the pseudorandom numbers generated by the <function tag="random">random</function> function and used by the <keyword tag="any">any</keyword> <glossary tag="keyword">keyword</glossary>. Using the same seed creates the same sequence of pseudorandom <glossary tag="value">values</glossary>.</p><p/><p>For example, if you call the <b>random</b> function five times to generate a list of five numbers, then change the <b>randomSeed</b> to another <function tag="value">value</function>, the next five <glossary tag="call">calls</glossary> to the <function tag="random">random</function> function will produce a different list of five numbers. However, if you set the <b>randomSeed</b> back to its original <function tag="value">value</function> and <command tag="call">call</command> the <function tag="random">random</function> function five more times, the list of five numbers is the same as the first list you generated.</p><p/><p>LiveCode uses a new <b>randomSeed</b> every time the <glossary tag="application">application</glossary> is started up.</p>
  </description>
</doc>