<doc>
  <legacy_id>3031</legacy_id>
  <name>resizeQuality</name>
  <type>property</type>
  <syntax>
    <example>set the resizeQuality of <i>image</i> to <i>qualityValue</i></example>
  </syntax>
  <library></library>
  <objects>
    <image/>
  </objects>
  <synonyms>
  </synonyms>
  <classification>
    <category>Images &amp; Multimedia</category>
  </classification>
  <references>
    <object tag="image">image Object</object>
  </references>
  <history>
    <introduced version="2.5.1">Added.</introduced>
    <changed version="2.8"></changed>
  </history>
  <platforms>
    <mac/>
    <windows/>
    <ios/>
    <android/>
  </platforms>
  <classes>
    <desktop/>
    <server/>
    <web/>
    <mobile/>
  </classes>
  <security>
  </security>
  <summary>Specifies which method LiveCode should use to scale an image.</summary>
  <examples>
    <example>get the resizeQuality of image "myImage"</example>
    <example>set the resizeQuality of the templateImage to "best"</example>
  </examples>
  <description>
    <p>The <b>resizeQuality</b> of an image takes one of three values:</p><p/><p>"normal": uses a "nearest" filter- i.e. no interpolation. This is the fastest scaling method and is the default for new images</p><p>"good": uses a bilinear filter- i.e. interpolation is done between the four nearest pixels using bilinearity.</p><p>"best": uses a bicubic filter- i.e.interpolation is done using cubic approximation from near pixels. This is the slowest scaling method but produces the best quality scaling.</p><p/><p>The default value of the <b>resizeQuality</b> for new images is "normal"</p><p/><p><b>Note:</b> Setting the <b>resizeQuality</b> of an image to any value other than "normal", "good" or "best" has no affect.  It will not produce an error, but the value of the property will remain as it was before.</p>
  </description>
</doc>