<doc>
	<legacy_id></legacy_id>
	<name>sessionCookieName</name>
	<type>property</type>

	<syntax>
		<example>set the sessionCookieName to <i>cookieName</i></example>
	</syntax>

	<synonyms>
	</synonyms>

	<summary>Specifies the name of the cookie used to store the session id.</summary>

	<examples>
<example>set the sessionCookieName to tCookieName</example>
<example>set the sessionCookieName to &quot;MYSESSION&quot;</example>
	</examples>

	<history>
		<introduced version="5.0">Added.</introduced>
		<deprecated version=""></deprecated>
		<removed version=""></removed>
		
	</history>

	<objects>
		<global/>
	</objects>

	<platforms>
		<mac/>
		<windows/>
		<linux/>
	</platforms>

	<classes>
		<server/>
	</classes>

	<security>
	</security>


	<classification>
	</classification>

	<references>
		<property tag="sessionSavePath">sessionName Property</property>
		<property tag="sessionID">sessionID Property</property>
		<property tag="sessionLifetime">sessionLifetime Property</property>
	</references>

	<description>
	<overview>Use the <b>sessionCookieName</b> property to set the name of the cookie used to store the session id.</overview>

		<parameters>
  <parameter>
    <name>cookieName</name>
    <description>a string specifying the name of the cookie</description>
  </parameter>		</parameters>

	<value></value>
	<comments>Use the <b>sessionCookieName</b> property to set the name of the cookie used to store the session id. When a session is created on the server, a cookie is created in the browser to identify the ongoing session. Setting the <b>sessionCookieName</b> is useful if you want to have multiple session running a the same time for a given website.<p></p><p>If the <b>sessionCookieName</b> is not specified then the default, LCSESSION, is used.</p><p></p><p></p><note>You do not need to alter any of the session properties in order to start and use sessions. They provide a way for advanced users to configure the way session work.</note><p></p><p></p><note>The <b>sessionCookieName </b>property is only available when running in CGI mode (Server).</note></comments>
	</description>
</doc>