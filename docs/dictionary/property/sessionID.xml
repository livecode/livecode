<doc>
	<legacy_id></legacy_id>
	<name>sessionID</name>
	<type>property</type>

	<syntax>
		<example>set the sessionID to <i>sessionID</i></example>
	</syntax>

	<synonyms>
	</synonyms>

	<summary>Specifies the identifier of the session to be started.</summary>

	<examples>
<example>set the sessionID to tMySessionID</example>
<example>set the sessionID to &quot;SessionIdentifier&quot;</example>
	</examples>

	<history>
		<introduced version="5.0">Added.</introduced>
		<deprecated version=""></deprecated>
		<removed version=""></removed>
		
	</history>

	<objects>
		<global/>
	</objects>

	<platforms>
		<mac/>
		<windows/>
		<linux/>
	</platforms>

	<classes>
		<server/>
	</classes>

	<security>
	</security>


	<classification>
	</classification>

	<references>
		<property tag="sessionSavePath">sessionSavePath Property</property>
		<property tag="sessionName">sessionName Property</property>
		<property tag="sessionLifetime">sessionLifetime Property</property>
	</references>

	<description>
	<overview>Use the <b>sessionID</b> property to set the identifier of the session to be started.</overview>

		<parameters>
  <parameter>
    <name>sessionID</name>
    <description>the string used as the session identifier</description>
  </parameter>		</parameters>

	<value></value>
	<comments>Use the <b>sessionID</b> property to set the identifier of the session to be started. <p></p><p>If the <b>sessionID</b> property is not set before starting a session, and the session cookie does not contain a session id, then a session id is automatically created. </p><p></p><p>If the current session has an automatically assigned id you can retrive it by getting the <b>sessionID</b> property.</p><p></p><p></p><note>You do not need to alter any of the session properties in order to start and use sessions. They provide a way for advanced users to configure the way session work.</note><p></p><p></p><note>The <b>sessionID </b>property is only available when running in CGI mode (Server).</note></comments>
	</description>
</doc>