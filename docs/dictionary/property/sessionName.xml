<doc>
	<legacy_id></legacy_id>
	<name>sessionName</name>
	<type>property</type>

	<syntax>
		<example>set the sessionName to <i>cookieName</i></example>
	</syntax>

	<synonyms>
	</synonyms>

	<summary>Specifies the name of the cookie used to store the session id.</summary>

	<examples>
<example>set the sessionName to tCookieName</example>
<example>set the sessionName to &quot;MYSESSION&quot;</example>
	</examples>

	<history>
		<introduced version="5.0">Added.</introduced>
		<deprecated version=""></deprecated>
		<removed version=""></removed>
		
	</history>

	<objects>
		<global/>
	</objects>

	<platforms>
		<mac/>
		<windows/>
		<linux/>
	</platforms>

	<classes>
		<server/>
	</classes>

	<security>
	</security>


	<classification>
	</classification>

	<references>
		<property tag="sessionSavePath">sessionName Property</property>
		<property tag="sessionID">sessionID Property</property>
		<property tag="sessionLifetime">sessionLifetime Property</property>
	</references>

	<description>
	<overview>Use the <b>sessionName</b> property to set the name of the cookie used to store the session id.</overview>

		<parameters>
  <parameter>
    <name>cookieName</name>
    <description>the name of the cookie</description>
  </parameter>		</parameters>

	<value></value>
	<comments>Use the <b>sessionName</b> property to set the the name of the cookie used to store the session id.<p></p><p>If the <b>sessionName</b> is not specified then the default, LCSESSION, is used.</p></comments>
	</description>
</doc>