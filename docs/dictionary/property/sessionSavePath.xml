<doc>
	<legacy_id></legacy_id>
	<name>sessionSavePath</name>
	<type>property</type>

	<syntax>
		<example>set the sessionSavePath to <i>folderPath</i></example>
	</syntax>

	<synonyms>
	</synonyms>

	<summary>Specifies where on the filesystem session data is stored.</summary>

	<examples>
<example>set the sessionSavePath to myFolder</example>
<example>set the sessionSavePath to &quot;/home/joe&quot;</example>
<example><p>set the sessionSavePath to empty</p></example>
	</examples>

	<history>
		<introduced version="5.0">Added.</introduced>
		<deprecated version=""></deprecated>
		<removed version=""></removed>
		
	</history>

	<objects>
		<global/>
	</objects>

	<platforms>
		<mac/>
		<windows/>
		<linux/>
	</platforms>

	<classes>
		<server/>
	</classes>

	<security>
	</security>


	<classification>
	</classification>

	<references>
		<property tag="sessionName">sessionName Property</property>
		<property tag="sessionID">sessionID Property</property>
		<property tag="sessionLifetime">sessionLifetime Property</property>
	</references>

	<description>
	<overview>Use the <b>sessionSavePath</b> property to set the path to the location on the server filesystem where session data is stored.</overview>

		<parameters>
  <parameter>
    <name>folderPath</name>
    <description>the path to a folder</description>
  </parameter>		</parameters>

	<value></value>
	<comments>Use the <b>sessionSavePath</b> property to set the path to the location on the server filesystem where session data is stored. If the <b>sessionSavePath</b> property is not set then the standard temporary folder is used.<p></p><p>To set the <b>sessionSavePath</b> back to the default set the <b>sessionSavePath</b> to empty.</p><p></p><p></p><note>You do not need to alter any of the session properties in order to start and use sessions. They provide a way for advanced users to configure the way session work.</note><p></p><p></p><note>The <b>sessionSavePath </b>property is only available when running in CGI mode (Server).</note></comments>
	</description>
</doc>