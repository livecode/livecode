<doc>
  <legacy_id>3491</legacy_id>
  <name>systemPrintSelector</name>
  <type>property</type>
  <syntax>
    <example>set the systemPrintSelector to {true | false}</example>
    <example>get the systemPrintSelector</example>
  </syntax>
  <library></library>
  <objects>
    <global/>
  </objects>
  <synonyms>
  </synonyms>
  <classification>
    <category>Printing</category>
  </classification>
  <references>
    <command tag="answer printer">answer printer Command</command>
  </references>
  <history>
    <introduced version="2.9">Added.</introduced>
  </history>
  <platforms>
    <mac/>
    <windows/>
    <linux/>
  </platforms>
  <classes>
    <desktop/>
    <server/>
  </classes>
  <security>
  </security>
  <summary>Reports whether to use LiveCode stacks or system dialogs for printer settings.</summary>
  <examples>
    <example>if the platform is "linux" then</p><p>  set the systemPrintSelector to false</p><p>end if</example>
  </examples>
  <description>
    <p>Use the <b>systemPrintSelector</b> property to specify whether LiveCode's print dialogs or the system default dialogs should be used by the <command tag="answer printer">answer printer</command> and <command tag="answer page setup">answer page setup</command> commands.</p><p/><p><b>Value:</b></p><p>The <b>systemPrintSelector</b> is either "true" or "false". The default is "false" on platforms that don't support native print dialogs, but "true" otherwise.</p><p/><p>If the <b>systemPrintSelector</b> is set to false, then the <command tag="answer printer">answer printer</command> command will use a stack called "Print Dialog" and the <command tag="answer page setup">answer page setup</command> command will use a stack called "Page Setup Dialog".</p><p/><p/>
  </description>
</doc>