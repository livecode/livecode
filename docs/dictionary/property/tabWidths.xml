<doc>
	<legacy_id></legacy_id>
	<name>tabWidths</name>
	<type>property</type>

	<syntax>
		<example>set the tabWidths [of line <i>lineNumber</i>] of <i>field</i> to <i>tabWidthList</i></example>
	</syntax>

	<synonyms>
	</synonyms>

	<summary>Specifies the width of columns in a field.</summary>

	<examples>
<example>set the tabWidths of field &quot;Comments&quot; to 20,20,20,0</example>
<example>set the tabWidths of field 1 to 40,80,120,160,200</example>
<example><p>set the tabWidths of line 2 to -2 of field &quot;list&quot; to tColumnWidths</p></example>
	</examples>

	<history>
		<introduced version="5.5 Added">5.5 Added</introduced>
		<deprecated version=""></deprecated>
		<removed version=""></removed>
		<changed version=""></changed>
	</history>

	<objects>
		<field/>
	</objects>

	<platforms>
		<mac/>
		<windows/>
		<linux/>
		<ios/>
		<android/>
	</platforms>

	<classes>
		<desktop/>
		<server/>
		<web/>
		<mobile/>
	</classes>

	<security>
	</security>


	<classification>
	</classification>

	<references>
		<property tag="hGrid">hGrid Property</property>
		<property tag="vGrid">vGrid Property</property>
		<property tag="borderWidth">borderWidth Property</property>
		<property tag="tabStops">tabStops Property</property>
		<message tag="tabKey">tabKey Message</message>
	</references>

	<description>
	<overview>Use the <b>tabWidths</b> property to set the widths of columns in a field.</overview>

		<parameters>
		</parameters>

	<value>The <b>tabWidths</b> of a field is a list of one or more positive integers, separated by commas.</value>
	<comments>The <b>tabWidths</b> consists of one or more integers separated by commas. Each integer is the width in pixels of column. The <b>tabWidths</b> returns the relative widths of columns whereas the <property tag="tabStops">tabStops</property> property returns the absolute positions of tabs.<p></p><p>Setting the <b>tabWidths</b> of a field to 20,20,20 will result in tab stops being placed at 20, 40 and 60 pixels.</p></comments>
	</description>
</doc>