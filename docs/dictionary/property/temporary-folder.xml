<doc>
	<legacy_id></legacy_id>
	<name>temporary folder</name>
	<type>property</type>

	<syntax>
		<example>the temporary folder</example>
	</syntax>

	<synonyms>
	</synonyms>

	<summary>Returns the path to the folder used for storage of temporary files.</summary>

	<examples>
<example>get the temporary folder</example>
<example>put the temporary folder &amp; &quot;/log.txt&quot; into tTempLog</example>
	</examples>

	<history>
		<introduced version="5.0.2">Added.</introduced>
		<deprecated version=""></deprecated>
		<removed version=""></removed>
		
	</history>

	<objects>
	</objects>

	<platforms>
		<mac/>
		<windows/>
		<linux/>
		<ios/>
	</platforms>

	<classes>
		<desktop/>
		<mobile/>
	</classes>

	<security>
	</security>


	<classification>
	</classification>

	<references>
		<function tag="specialFolderPath">specialFolderPath Function</function>
		<property tag="engine folder">the engine folder Property</property>
		<property tag="home folder">the home folder Property</property>
		<property tag="desktop folder">the desktop folder Property</property>
		<property tag="documents folder">the documents folder Property</property>
	</references>

	<description>
	<overview>Returns the path to the folder used for the storage of temporary files.</overview>

		<parameters>
		</parameters>

	<value></value>
	<comments>On Mac OS X, Windows and Linux <b>the temporary folder</b> property returns the path to the folder used for the storage of temporary files.<p></p><p>On iOS <b>the temporary folder </b>property returns the folder in which the application should store any temporary data that is not needed between launches (this folder is not backed up by iTunes on sync).</p><p></p><p>This property is read only and is equivalent to the specialFolderPath(&quot;temporary&quot;) function.</p></comments>
	</description>
</doc>