<doc>
	<legacy_id>2351</legacy_id>
	<name>textFont</name>
	<type>property</type>

	<syntax>
		<example>set the textFont of <i>object</i> to <i>fontName</i>[,<i>language</i>]</example>
		<example>set the textFont of [<i>chunk</i> of] <i>field</i> to <i>fontName</i>[,<i>language</i>]</example>
	</syntax>

	<synonyms>
	</synonyms>

	<summary>Specifies the <glossary tag="font">font face</glossary> of text in an <glossary tag="object">object</glossary>.</summary>

	<examples>
<example>set the textFont of button &quot;Hello&quot; to &quot;Courier&quot;</example>
<example>set the textFont of the foundChunk to &quot;Comic Sans&quot;</example>
<example><p>set the textFont of field &quot;Input&quot; to &quot;Arial,Japanese&quot;</p></example>
<example>set the textFont of button &quot;Hello&quot; to &quot;Courier&quot;</example>
<example>set the textFont of field &quot;Text&quot; to &quot;Japanese&quot;</example>
	</examples>

	<history>
		<introduced version="1.0">Added.</introduced>
		<deprecated version=""></deprecated>
		<removed version=""></removed>
		<changed version="5.5">language tag removed, no longer sets textSize,textStyle,textHeight</changed>
<changed version="2.0">2.0</changed>
	</history>

	<objects>
		<stack/>
		<card/>
		<group/>
		<field/>
		<button/>
		<graphic/>
		<scrollbar/>
		<player/>
		<image/>
	</objects>

	<platforms>
		<mac/>
		<windows/>
		<linux/>
		<ios/>
		<android/>
	</platforms>

	<classes>
		<desktop/>
		<server/>
		<web/>
		<mobile/>
	</classes>

	<security>
	</security>


	<classification>
		<category>Appearance &amp; Positioning</category>
	</classification>

	<references>
		<property tag="printTextFont">printTextFont Property</property>
		<property tag="allowInlineInput">allowInlineInput Property</property>
		<property tag="printFontTable">printFontTable Property</property>
		<property tag="HTMLText">HTMLText Property</property>
	</references>

	<description>
	<overview>Use the <b>textFont</b> <glossary tag="property">property</glossary> to change the appearance of text.</overview>

		<parameters>
		</parameters>

	<value>The <b>textFont</b> of an <glossary tag="object">object</glossary> or <glossary tag="chunk">chunk</glossary> is either empty or a <keyword tag="string">string</keyword>.</value>
	<comments>Setting the <b>textFont</b> of an <glossary tag="object">object</glossary> to empty allows the <b>textFont</b> of the <glossary tag="object">object's</glossary> <property tag="owner">owner</property> to show through. Use the <keyword tag="effective">effective</keyword> <glossary tag="keyword">keyword</glossary> to find out what <glossary tag="font">font</glossary> is used for the <glossary tag="object">object</glossary>, even if its own <b>textFont</b> is empty.<p></p><p>If the <i>chunk</i> contains more than one font, the <glossary tag="chunk">chunk's</glossary> <b>textFont</b> <glossary tag="property">property</glossary> reports &quot;mixed&quot;.</p><p></p><p>On Mac OS systems, if the specified font isn't available, the system font (which is set in the Appearance control panel and specifies the font used for menus) is used. On Unix systems, if the specified font isn't available, Helvetica is used. On Windows systems, if the specified font isn't available, the current Windows font is used.</p></comments>
	</description>
</doc>