<doc>
	<legacy_id>2291</legacy_id>
	<name>textHeight</name>
	<type>property</type>

	<syntax>
		<example>set the textHeight of <i>field</i> to <i>pixels</i></example>
	</syntax>

	<synonyms>
	</synonyms>

	<summary>Specifies the amount of space between lines of text in a <keyword tag="field">field</keyword>.</summary>

	<examples>
<example>get the effective textHeight of field &quot;Results&quot;</example>
<example>set the textHeight of field 1 to theLines * the textHeight of field 1</example>
<example>set the textHeight of field 1 to 20</example>
	</examples>

	<history>
		<introduced version="1.0">Added.</introduced>
		<deprecated version=""></deprecated>
		<removed version=""></removed>
		<changed version="5.0">No longer causes other text properties to be set.</changed>
	</history>

	<objects>
		<field/>
	</objects>

	<platforms>
		<mac/>
		<windows/>
		<linux/>
		<ios/>
		<android/>
	</platforms>

	<classes>
		<desktop/>
		<server/>
		<web/>
		<mobile/>
	</classes>

	<security>
	</security>


	<classification>
		<category>Appearance &amp; Positioning</category>
	</classification>

	<references>
		<function tag="textHeightSum">textHeightSum Function</function>
		<property tag="fixedLineHeight">fixedLineHeight Property</property>
		<property tag="printTextHeight">printTextHeight Property</property>
		<keyword tag="effective">effective Keyword</keyword>
	</references>

	<description>
	<overview>Use the <b>textHeight</b> <glossary tag="property">property</glossary> to change the <glossary tag="appearance">appearance</glossary> of text in a <keyword tag="field">field</keyword>.</overview>

		<parameters>
		</parameters>

	<value>The <b>textHeight</b> of a <keyword tag="field">field</keyword> is an <keyword tag="integer">integer</keyword> greater than or equal to 4.<p></p><p>By default, the <b>textHeight</b> <glossary tag="property">property</glossary> of newly created <glossary tag="field">fields</glossary> is set to 4/3 times the <property tag="textSize">textSize</property> of the <keyword tag="field">field</keyword>.</p></value>
	<comments>The <b>textHeight</b> <glossary tag="property">property</glossary> determines how tall each line of a <keyword tag="field">field</keyword> is, and is equal to the vertical distance in <property tag="pixels">pixels</property> between the baseline of one line and the baseline of the next. The minimum <b>textHeight</b> is 4 <property tag="pixels">pixels</property>.<p></p><p></p><note>If you change the field's <b>textSize</b>, LiveCode automatically sets the <b>textHeight</b> to<code tag=""> trunc(4/3 * the textSize of </code><i>field</i>). For example, if the <b>textSize</b> is set to 12, the <b>textHeight</b> is set to 16 by <keyword tag="default">default</keyword>. If the textSize is set to 14, the <b>textHeight</b> is set to 18. So, if you want to change the <b>textHeight</b> and <property tag="textSize">textSize</property>, set the <property tag="textSize">textSize</property> first, then set the <b>textHeight</b>.</note><p></p><p>If a field's <b>fixedLineHeight</b> <glossary tag="property">property</glossary> is false, the <b>textHeight</b> of the <keyword tag="field">field</keyword> has no effect: in this case, the text height of each line is determined by the size of the largest text in the line, and changing the <b>textHeight</b> doesn't change the spacing of lines.</p></comments>
	</description>
</doc>