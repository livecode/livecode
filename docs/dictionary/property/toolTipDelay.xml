<doc>
  <legacy_id>2507</legacy_id>
  <name>toolTipDelay</name>
  <type>property</type>
  <syntax>
    <example>set the toolTipDelay to <i>milliseconds</i></example>
  </syntax>
  <library></library>
  <objects>
    <global/>
  </objects>
  <synonyms>
  </synonyms>
  <classification>
    <category>User Interaction</category>
  </classification>
  <references>
    <property tag="toolTip">toolTip Property</property>
  </references>
  <history>
    <introduced version="1.0">Added.</introduced>
  </history>
  <platforms>
    <mac/>
    <windows/>
    <linux/>
  </platforms>
  <classes>
    <desktop/>
    <web/>
  </classes>
  <security>
  </security>
  <summary>Specifies how long the user must <keyword tag="point">point</keyword> to an <glossary tag="object">object</glossary> with the <glossary tag="mouse pointer">mouse pointer</glossary> before its <glossary tag="tool tip">tool tip</glossary> appears.</summary>
  <examples>
    <example>set the toolTipDelay to 200 <code><i>-- faster-than-default tooltip</i></code></example>
    <example>set the toolTipDelay to 0 <code><i>-- no tooltip</i></code></example>
  </examples>
  <description>
    <p>Use the <b>toolTipDelay</b> <glossary tag="property">property</glossary> to control the responsiveness of <glossary tag="tool tip">tool tips</glossary>.</p><p/><p><b>Value:</b></p><p>The <b>toolTipDelay</b> is a <href tag="../glossary/nondashnegative.xml">non-negative</href> <keyword tag="integer">integer</keyword>.</p><p/><p>By default, the <b>toolTipDelay</b> is 500 (half a second).</p><p/><p><b>Comments:</b></p><p>A tool tip is a small box containing a line or two of text, which pops up on the screen when the mouse pointer hovers over a control. Use the <b>toolTip</b> <glossary tag="property">property</glossary> to set the contents of the <glossary tag="tool tip">tool tip</glossary>.</p><p/><p>The mouse must be still for the <b>toolTipDelay</b> time or the <glossary tag="tool tip">tool tip</glossary> will not appear. If the mouse remains within a <keyword tag="control">control</keyword>, but is constantly moving, the <b>toolTipDelay</b> time does not start until the mouse stops moving.</p><p/><p>If the <b>toolTipDelay</b> is zero, the <glossary tag="tool tip">tool tip</glossary> is disabled and does not appear.</p>
  </description>
</doc>