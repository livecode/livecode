Name: virtual property

Synonyms: virtual property,virtual properties,virtual custom property,virtual custom properties

Type: glossary

Description:
A <custom property> for which an <object> has a <getProp> or <setProp> <handler>, but which is never attached to the <object>.

References: getProp (control_st), setProp (control_st), object (object), custom property (glossary), handler (glossary)

Tags: properties
