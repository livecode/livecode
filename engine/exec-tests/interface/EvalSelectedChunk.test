setup
create field

set the text of the last field to "abcd"

select char 1 to -1 of the last field
test the selectedChunk is "char 1 to 4 of field 1"

select empty
test the selectedChunk is empty

teardown
