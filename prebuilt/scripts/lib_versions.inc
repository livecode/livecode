# This file is to be sourced in any of the scripts/build-<lib> script,
# as well as in fetch-libraries.sh.
# That allows a simple update of both to-build and to-fetch libraries.
# Only Windows is left apart, since it has its own scripts.

# OpenSSL
OpenSSL_VERSION=1.0.1p

# ICU
ICU_VERSION=55.1
ICU_VERSION_MAJOR=55

# CURL
Curl_VERSION=7.43.0

# CEF
CEF_VERSION="3.2062.1796"
