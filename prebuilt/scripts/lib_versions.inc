# This file set the libraries version variables to the version defined in versions/

OpenSSL_VERSION=$(cat versions/openssl)
ICU_VERSION=$(cat versions/icu)
ICU_VERSION_MAJOR=$(cat versions/icu_major)
Curl_VERSION=$(cat versions/curl)
CEF_VERSION=$(cat versions/cef)
